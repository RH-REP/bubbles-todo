# 開発メモ — 未決定・相談中・申し送り

**最終更新**: 2026-08-31（海のズームの回）

相談が終わる前に次の指示が来ることがあるので、**宙に浮いた論点はここに置く**。
決まったものはこのファイルから消し、`README.md` / `HANDOFF.md` に移す。

- 仕様そのもの → `README.md`
- 同じ穴に落ちないための申し送り → `HANDOFF.md`
- **まだ決まっていないこと → このファイル**

> ⚠️ `README.md` と `HANDOFF.md` は**まだ3タブ時代の記述のまま**で、いまの実装（6タブ・タグの海・完了の海・作業ログ）を反映していない。書き換えの許可待ち。

---

## A. 決めてもらう必要があるもの

### A-19. その日の海に直に書ける（利用者の指示）

今日の画面に入力欄が無く、**海に書いてからタブへ運ぶ**しか道が無かった。
面の**兄弟**として下に入力欄を置いた（面は flex:1 なので自然に縮み、
drift が測る clientHeight もそのまま正しい——海のように重ねると
床の高さを CSS 変数で渡す配管が要る）。

- 書いたものは**いま映している日**に入る（今日でも、明日でも）
- **タグもその場で付く。**入力欄に触ると札が開く。ふだんは畳む
  （常に出すと、書く前から選択肢が並んで水面が狭くなる）
- **過ぎた日には出さない**（`hidden`）。過去はその日の記録なので足させない
- 札に出さないタグが2つ：**今日**（映している日に入るので重ねる意味が無い）と
  **完了**（これから書くものに付ける意味が無い）

決めたが、覆してよいこと：

- **選んだタグは送ったあとも残す。**同じタグのものを続けて書くのが楽になるため。
  隠し持ってはいない（札は出たままなので、何が付くかは目で見える）。
  毎回まっさらにするほうがよければ `picked.clear()` を足すだけ
- **海にある時計ボタン（書いてすぐ5分）は、こちらには置いていない。**
  ご指示に無かったため。要るなら同じものを移植できる


### A-18.【解決】盤の「完了」が、一手を書き終えた合図に読めていた

利用者の指摘。前は入力欄「次の一手」のすぐ下に `[はじめた][完了]` が横並びで、
**入力欄の直下の「完了」が OK に読めた**。押すと項目そのものが完了になるので、
取り違えると重い。並びを組み替えた：

```
[ はじめた ][ OK ]      ← OK は「次の一手」を書き留めるだけ。盤は閉じない
[ タスク完了 ]
──────────
[ 消す ]
```

- OK は他のボタンと**性質が違う**（盤を閉じない）。何も起きなかったように
  見せないため、押すと少しのあいだ「書き留めた」に変わる。入力欄の焦点も外す
  （端末のキーボードが引っ込む）
- 完了は「タスク完了」と名乗り直し、**消すのすぐ上**へ。取り返しの重いものが
  下にまとまり、入力欄からは離れる
- 完了済みの上では、いままでどおり「海にもどす」に入れ替わる（実測ずみ）


### A-17. 今日の海を日付ごとに持つ — 決めた前提と、残っている穴

利用者の指示で `t.today`（真偽値）を **`t.days`（'YYYY-MM-DD' の配列）**に替えた。
`t.today` は残してあるが、**days から作り直す控え**（画面30か所はそのまま動く）。
書き換えてよいのは `syncTodayFlags()` と `setDay()` だけ。

**「持ち越さない」は保たれている。**明日の海が空なのは、明日のキーを持つものが
まだ無いからで、勝手に運ばれることはない。`rollover()` はもう today を落とさない
（戻す動作が無くなったので、戻り値はいつでも 0。呼び手2か所とも見ていない）。

決めたこと（利用者の判断）：

- **過去は全部出す。見た目は変えない。**着手したものを強調も、しなかったものを
  沈めもしない。件数も出さない。並べ替えもしない
  ——順番が違うこと自体が「こちらはやっていない」の印になるため
- **未来の導線は見出しの日付**。押すと 昨日/今日/明日/明後日/週末/1週間先 が選べる。
  遠い日は左右になぞって行く（右へ＝過去、左へ＝先）
- **落とし先は「今日の画面がいま映している日」。**過去を映している間は受け付けない
  （`dropDay()` が null を返し、海が「過ぎた日には足せない」と言う）

残っている穴：

- ~~落とし先が画面をまたいで効くのに、タブは「今日」と書いたまま~~
  **【解決】**利用者の指示で、**タブ自身が日付を出す**ようにした。
  今日以外を映しているとき、下タブの文字が「今日」→「8/31」に変わる。
  さらに**「今日」タブの長押しでその場から日を選べる**（450ms。指が 8px 動いたら取り消し。
  開いたあとの click は捨てるので、選ぼうとしただけで面が変わることはない）。
  ＝ 海に居るまま落とし先を決められる。
  色では言わない——`--accent-a` は下タブの地に対し 明 4.27 / 暗 3.87 で 4.5:1 を割る。
  文字そのものが変わるので色は要らず、`--text`（明 15.96 / 暗 14.07）＋太字＋細い下線にした。
  さらに利用者の指示で、**「今日」の横に ▽ を置いて長押しを示唆**している
  （海の「▽しぼる」と同じ記号・同じ意味＝押すと何か出る。この画面で記号を増やさない）。
  ▽ は aria-hidden で読み上げには乗せず、できることは title で添える
- 過去の日でも、バブルを選べば完了・消すはできる（項目そのものへの操作なので）。
  その日の顔ぶれから外すことだけを塞いでいる
- `todayLogs` に `day` を足したので、未来へ置いたぶんは**その日**で数える。
  ただし **`day` を持たない古い記録は、決めた時刻から日を出す**（前と同じ数え方）
- 選べる過去は「昨日」まで。それより前はなぞって行く
  ——遠い過去を一覧にすると、それ自体が「やらなかった日」を数える装置になる


### A-16. きっかけの日にち — 決めた前提と、まだ決めていないこと

利用者の指示で、きっかけ本体に「いつの日のものか」を持たせた。
**数え方は「その月の n 回目のその曜日」**（第2火曜＝2回目の火曜。利用者の判断）。
「最終」は4回目とは別物で、その曜日が5回ある月だけ違う日を指す。

**予定外のきっかけは隠す**（利用者の判断）。未分類はいつでも出す（受け皿なので）。
「ぜんぶ見る」で切り替えられる。この切り替えは**保存していない**——
開き直すと「今日のぶんだけ」に戻る。毎回押し直すのが煩わしければ保存に変えられる。

**過ぎた日のことは何も持たせていない。**「予定の日だったのに着手しなかった」を
表す状態を、データにもどこにも作っていない。作れば必ずどこかに出したくなり、
それは未処理の山になる（§0）。**これは意識して閉じた道であって、実装漏れではない。**

**【決着】時刻は入れない。**利用者の判断：「このままでよい」。
隔週・毎月n日・特定の日付も入れない。曜日と週だけ。
ぶら下がるバブルごとの日にちも持たない（カード単位）。

### A-16b.【決着】今日だけの枠を上へ出す

利用者の指示：

> 限定イベントはきっかけの上部に、今日の特別枠としてでるように

日にちを決めたきっかけのうち、**今日がその日のもの**を「今日だけ」の見出しの下へ
まとめて上に出す。毎日のもの（日にちを決めていない）は特別ではないので、下の並びのまま。
「ぜんぶ見る」のときも、上へ出るのは今日の日のものだけ（`store.anchorDue` を見る）。

**ここに来たカードは並べ替えの対象から外した。**場所を決めているのはユーザーではなく
「今日」なので、掴んで動かせると嘘になる。取っ手も、⋮ の 上へ／下へ も出さない
（色の印だけは残す——色は見分けの印で、並べ替えとは別のこと）。

**ついでに、前からあった並べ替えのずれを直した。**
`onReorderUp` は「表示上の行き先」をそのまま store のずらし量として渡していたので、
**予定の日でないきっかけが隠れていると、1つ多く／少なく動いていた**。
今日だけの枠を上へ出すとそれが常態になるので、
表示の並びから store の位置を引き直す（`storeDeltaFor`）形に変えた。
実測：特別枠1枚を上に出した状態で、2番目のカードを3番目の下へ運ぶと、
store の並びも表示の並びも狙いどおりに入れ替わる。

**見出しは小さく、囲まない。**大きく囲うと「今日の課題」の箱に見えて、
このアプリが避けている絵（未処理の山）に近づくため。


### A-1.【解決】中央の海が飽和する

**ズームアウトを付けた（利用者の指示）。**「引いて見る」ボタンを右上の2段目に置き、
押すたびに 1.00 → 0.80 → 0.60 → 1.00 と回る。

やり方は2つあった。**面ごと CSS の transform で縮める**案は、物理の面は広がるが
「指の座標 → 面の座標」の戻し計算（`drift.js` が rect の差で出している）が倍率ぶんずれる
——drift の座標系に手を入れることになる。採ったのは **1件ずつ直径に倍率を掛ける**案で、
物理も当たり判定も実寸のままなので座標の計算はどこも変わらない
（`drift.js` は `item.size` が `opts.size` に勝つ、の2行だけ）。

実測（420×880・中央15件）：

| 段 | 直径 | 面積の占有 | 文字 |
|---|---|---|---|
| 1.00 | 98〜136px | 53% | 13px |
| 0.80 | 78〜109px | **34%** | 9.6px |
| 0.60 | 59〜82px | **19%** | 7.2px |

0.60 でも最小直径は 46.8px（`MIN_D` 78 × 0.6）でタップ目標 44 を割らない。
引いた状態でのドラッグも実測ずみ（72px 動かして 72px 追従。ずれ無し）。

**状態は保存していない。**開き直すと 1.00 に戻る。小さいまま開いて理由が分からない、
を避けたが、毎回押し直すのが煩わしければ保存に変えられる（1行）。

### A-15.【解決】多タグのバブルが、止まっている間は1タグと見分けが付かない

**追記2（利用者の指示「位相を時間にランダムに」「割合も時間とともに」）：**
止まった扇は図に見えるので、境目を動かした。**色そのものは変えていない。**

- **位相** … 膜ごと `transform: rotate`。等速だと時計に見えるので、中継点を
  不揃いに置いて速さを揺らす。1周の長さ（58〜104秒）と開始位置は
  個体ごとに乱数（`--ts1` / `--ts2` を JS が作るときに1回だけ振る）
- **割合** … 扇の境目そのものを動かす。2色は 43%〜57%、3色は2つの境目を別々に。
  `@property` で角度に型を付けないと補間されない（パッと飛ぶ）

実測（時計を手で送って確認。この面は hidden で時計が進まないため）：
2色 a=155.4〜206度、3色 a=99.4〜140.2 / b=221.7〜263度、位相は1周。
どれも 0% と 100% が一致＝継ぎ目なし。3色の境目は最小 81.5度 空く（必要 36度）。

**重さの違いに注意。**位相は合成だけ（塗り直し無し）。
**割合のほうは conic を毎コマ塗り直す。**非力な端末で効くようなら
`tint-prop-2` / `tint-prop-3` を外せば、位相だけ残る。

**追記（利用者の指示「色はスパッと切れるのではなく、自然な境界で」）：**
最初は硬い境目にしていたが、羽をつけた（片側 18度・渡す幅 36度）。
ただ羽をつけただけでは境目が**灰色に沈む**（oklab の直線なので、色相が
真向かいの組では中点の彩度が元の 4〜12% まで落ちる）。
そこで境目に中間色を明示的に置き、それを `--bg` へ 30% 寄せた。
明では白っぽく、暗では暗く抜ける＝「膜が薄くなった場所」に見える。

色相を回して繋ぐ道（oklch shorter hue）は採らなかった。彩度は保てるが、
途中で**別のタグの色**を通る（今日 H50 → 瓶覗 H205 は緑を通る）。
つまみは `--tint-edge`（羽の幅）と `--tint-seam`（地へ寄せる量）。

実測：境目の色は純色より**高い**（明 7.50 / 暗 6.44 対 純色 7.00 / 5.04）。
`--bg` へ寄せる向きが、明暗どちらでも文字から遠ざかる側になるため。
下限は純色のままで動かない。

**時間で入れ替えるのをやめ、1つのバブルを色の数で分けた（利用者の指示）。**
2色なら半分ずつ、3色なら3等分。conic-gradient の**硬い境目**なので、
どの一点を取ってもタグの色そのままで、中間色はどこにも現れない
（「混ぜたではなく」は最初からの決めごとで、そこは変えていない。
　前に conic をやめたのは、柔らかい境目で空間的に混ぜていたため）。

副産物としてコントラストの下限が上がった。溶かしていたときは途中の補間色
（oklab。色相が真向かいだといったんクロマがほぼ 0 に落ちる）も測る必要があったが、
分けた今はどの画素も tf1 / tf2 / tf3 そのもの。下限は「いちばん弱い1色」で決まる。

### A-5.【解決済みだった】枠の中に行き先の点を出すか

**この項目は古い。**書かれた当時は点（marks）でタグを表していたが、
いまは4画面とも `store.tagsOf(id)` の色すべてでバブルを染めている
（`plan.js` / `gap.js` / `today.js` / `sea.js` の `tagColors()` は同じ実装）。
＝ きっかけの枠にある「今日でもあるもの」は、**今日の色を持って**そこに居る。

A-15 で色を空間的に分けたので、**止まっている間もひと目で分かる**ようになった。
別の手がかりを足す必要は無い。

### A-6.【解決】すきまの空き枠が、埋まっている枠より目立つ

輪の役目を「置ける場所の合図」に絞った。その役目はドラッグしている間にしか無い。
  ・平時 … 実線のヘアライン `--line`（明 .10 / 暗 .12）。場所だけ示して黙る
  ・掴んでいる間 … いままでの破線 `--line-strong` に戻す
前は 96px の破線を `--line-strong` で常時描いていた。破線は「まだ何か入るはず」と
読ませる書き方で、しかも大きく、**空きのほうが埋まりより強く見えていた**。
まだ埋めていない場所が並ぶ画面でそれが目立つのは、未達の山を作るのと同じ（§0）。

### A-7. 海タブへのドロップ（全解除）で、ユーザーのタグは落とさない —— **現状維持を推奨**

いまは `today` / `plan` / `gap` / アンカーを外し、ユーザーのタグ（仕事・プライベート等）は残す。

**そのままでよい**と考える。理由は、2種類のタグが別のことを言っているため：
  ・今日 / きっかけ / すきま … **いつ・どこで手をつけるか**（置き場所）
  ・仕事 / プライベート …… **それが何か**（性質）
海へ戻すのは「置き場所をまだ決めていない」であって「これは仕事ではなくなった」ではない。
またユーザーのタグには左右の海という別の外し口があり、
全部落とすと取り消しのトースト以外に戻す道が無い。

変えるなら1行（`clearAll()` で `store.setTag(id, t.id, false)` を回す）。

### A-8.【解決】端の札の下のバブルは、その一点では掴めない

**A-9 の矢印看板がそのまま直しになった。**案内が面の中の
**バブルより後ろ**へ移ったので、上にバブルが来ても指はバブルに当たる。
ふだんは端の札（`.sea-edge`、バブルより前）を出さなくなった。
あれが前に出るのは**バブルを掴んでいる間だけ**で、そのときは指がバブルを持っているので
札の下のバブルを掴む場面がそもそも無い。

### A-9.【解決】中央から隣の面へ移る手がかりが、背景をなぞる以外に無い

**矢印看板を足した（利用者の指示）。**行ける向きすべてに、矢印とその海の名前。
中央からは「↑完了」「←仕事」「→プライベート」、タグの海からは「→ぜんぶ」。

置き場所が肝で、**面の中の、バブルより後ろ**（面のいちばん先頭に足す＝先に描かれる）。
  ・後ろにあるので、上にバブルが来ても指はバブルに当たる（＝ A-8 の直し）
  ・面と一緒に動くので、なぞっている最中も景色として付いてくる
  ・空いているところを押せば移れる（押す的が無くなった問題も同時に解ける）
  ・そこから指を滑らせれば、ふつうに面が動く（`isBackground` が `.sea-sign` を例外にし、
    指が 6px 以上動いたら看板の click を捨てる）

**残る難点：海が混んでいると看板がバブルに隠れる。**背後に置いた以上これは避けられない。
A-1 のズームアウトで空きが増えたぶんは緩和されている。

### A-10.【解決】タグの海だけ「中央へ戻る札」を残していた

ご質問への答え：**あれはタグの解除ではなく、ただの行き先案内**でした。
タグの海（左＝仕事など）にいるとき、中央の「ぜんぶ」の海へ戻るための押す的です。
バブルには何もしません。いまは矢印看板「→ぜんぶ」に置き換わりました。

**タグの管理は盤（バブルを選んだ状態）に付けた（利用者の指示）。**
タグ1つ＝ボタン1つで、押している／いないを `aria-pressed` で持つ。
  ・書くのは**閉じるとき**。押した瞬間に書くと店が emit し、画面が組み直されて
    いま中央に居るノードごと作り直される＝1つ付けたところで盤が閉じる。
    次の一手・リンクと同じで、溜めてから閉じるときにまとめて書く
  ・**「完了」は出さない。**専用のボタンが下にあり、音と取り消しが付いている。
    同じことを別の顔で二度できるようにすると、どちらが本筋か分からなくなる
  ・付いていないほうを薄くするのではなく、**付いているほうを起こす**（§0）

### A-13.【決着 → 状況が変わったので再決着】完了の海へのドロップ

最初の決着：**「完了に送りやすい代わりのトレードオフなのでよい」**（完了が上に在った頃）。

そのあと利用者の指示で **上＝長期保留 / 下＝完了（固有枠）** になり、前提が変わった。
いまは**端へのドロップで完了にはできない**。理由は2つ。

1. **場所** … 下の帯はタブバーのすぐ上（`bottom:64px` の 21px 厚）。
   「今日」タブへバブルを運ぶ指がその帯の上を通る。離す位置が数十 px ずれただけで
   完了になるのは、取り消せるとしても筋が悪い
2. **決め方** … 完了は「訊かれて答えるもの」ではなく
   「自分から押したいときに押すもの」（README）。端は見に行く口にとどめる

実装は `sea.js` の `DROP_DIRS = ['up','left','right']` 1か所。
`edgeHitAt()`（当たり判定）と `renderEdges()`（掴んでいる間の札）が同じ並びを読むので、
「光っているのに付かない」は起きない。実測：掴んでいる間、上・左・右の3本だけ札が出る。

**上も固有枠にした**（利用者の指示）。上=長期保留 / 下=完了 で、左右だけが取り合い。
どちらも「いまの海から外したもの」の行き先なので、
外した先が譲られて消えると、外す操作そのものが行き止まりになる。

**残る非対称**：ほかの海は端へ落とせば付くのに、完了だけは落とせない。
完了にする口は 盤の [タスク完了] だけ。ここは指示待ち——
「下も落とせるようにする」なら `DROP_DIRS` に `'down'` を足すだけで戻せる。

### A-20.【要確認】「ならべる」は契約 §7 の「全件」から長期保留を外した

契約 §7 は「**ならべるは全件が並ぶ**」。完了だけを外していた。
そこへ長期保留を足したので、判断が要った。

**外した。**この盤の見出しは「古い順 — 放置しているものが先頭」で、
長期保留＝自分で「いまは見ない」と決めたものが、その先頭に並ぶ。
それは README が作らないと決めているもの——「やっていないことの山を、
罰として突き返す」——そのものになる。完了を外しているのと同じ理由の外し方。

実測：長期保留を1件立てると 15件 → 14件。上の海には出る。
`js/screens/sea.js` の `renderGrid()`。**契約からの逸脱なので、差し戻すならここ1か所。**

### A-23.【解決】盤の [OK] が飾りになっていた（利用者の指摘）

A-18 で「次の一手を書き留めるのは [OK]」と決めたのに、
**入力欄は打つたびに 400ms 後と blur で `setFirstStep` を書いていた**。
つまり OK を押さなくても更新されていて、明示の約束が守られていなかった。

直した形：

  ・打った文字は **「書きかけ」（draft）へ控えるだけ**。記録には触らない
  ・記録に触るのは **[OK] だけ**。押すと `commitStep` が1件積む
  ・盤を閉じるときも書きかけへ控える（打った文字が消えるのは事故なので）

**代償**：次の一手を**空にして消すことはできなくなった**（置き換えはできる）。
`commitStep` は next が空だと積まない——git のコミットが必ず次を指すのと同じ規則で、
store 側の既存の決まり。消せるようにするなら、そこも一緒に変えることになる。

[OK] は積めなかったとき理由を出す（「次の一手が要る」「変わっていない」）。
赤くはしない——咎めるものではなく、まだ足りていないだけなので。

### A-24. 盤から記録を積めるようにした（作業メモ ＋ 次の一手）

利用者の指示。`store.commitStep` は前からあったが、呼んでいたのは
集中画面（5分）だけだった。盤にも **作業メモ** の欄を足して、
[OK] で **{作業メモ, 次の一手, 日時}** を1組で積む。**記録の入口は store の同じ口ひとつ**。

「履歴」のボタンは**箱を先に作って、中身が無い間だけ隠す**。
前は組み立て時に1件も無ければボタンごと作っていなかったので、
積んでも盤を開き直すまで出てこなかった。

欄の名前は3か所とも **作業メモ** にそろえた（盤・集中画面・履歴）。
前は履歴と集中画面が「今回なにをしてたか」で、同じものに2つ名前があった。

**残り**：履歴からは直せない（書き損じ直しは集中画面の `amendLastStep` のみ）。
記録を消す口はどこにも無い。

### A-25.【解決】行が増えて、盤が下タブへもぐりかけていた

作業メモ（+44px）と長期保留の日（+123px）で、いちばん背の高い形が **592px**。
実測 375×812 では余裕があるが、**375×667 では余りが 8px**、
375×568 では**入りきらない**（下タブへもぐる＝触れなくなる）。

履歴の面に前からある「高さの上限＋中だけスクロール」を、盤にも同じ形で入れた。
実測 568 では 496px で頭打ちになり、中でスクロールして [消す] まで届く。

### A-28.【解決】今日の海のさいころが、海のさいころと別の規則で引いていた

利用者の指摘：

> 今日の海は、実質すべての海のフィルター版である

そのとおりだったが、実装はそうなっていなかった。**同じ画面の中で、
水面とさいころが別々に候補を集めていた**：

| | 水面（itemsForField） | さいころ（pickList） |
|---|---|---|
| 見る日 | いま映している日 | **常に今日**（store.todays()） |
| 完了 | 今日は外す／過去はその日の記録として残す | 今日は外れる（過去は候補外だった） |
| 長期保留 | 出ていた | 出ていた |

結果、**明日を映していても、さいころは今日から引いていた**（実測ずみ）。
画面には明日のものしか無いのに、押すと今日のものが開く。

直した形は「海と同じ規則」：

  ・候補は**いま画面に浮かんでいるもの**から、完了したものを除いたぶん
    （海の pickFace() と同じ。完了の海にさいころが無いのと同じ理由）
  ・ボタンの文言も映している日で言う（「明日 8/31（月）から1つ選んで…」）

**長期保留はここでは外さない。**一度これも外したが、間違いだった。利用者の指摘：

> 日付の水面にあるものは、長期保留のフィルターで消さない。
> 長期保留は一種のタグで、すべての海ではデフォルトで非表示にするため

つまり「既定では出さない」は**海の側の決まり**（すべての海＝中央と、タグの海、
それに「ならべる」）であって、タグそのものの性質ではない。
日付の水面は**その日に置いたという記録**なので、タグの表示規則で消してよいものではない
——置いたのに消えると、置いた事実のほうが失われる。

**「フィルター版」が指していたのは候補の集め方**（水面とさいころが同じものを見ること）で、
表示規則まで海から持ち込むことではなかった。読み違えていた。

実測：明日が空ならさいころは押せない（前は今日から引けてしまった）。
明日に2件置くとそのどちらかが開く。
長期保留にしても今日の水面は 4件のまま、同時に中央の海は 15→14 で上の海に1件出る。

### A-26. すきまを作り替えた（1枠1個の反転と、実装の統合）

利用者の指示。**明示的な反転**が1つ入っている：

> 1枠1個の決まりは変わりません（2個目を落としたら古いほうが未分類へ静かに移る）

これを外した。いまは1枠に何個でも入り、1件が複数の枠にも入る。
押し出しの仕組み（`pushedOut`）ごと無くなった。
「耳だけ空いている今、迷わず1つ」という中心の考え方は、この画面から消えている。

**実装は1つに寄せた。**きっかけの実装を `js/screens/cardlist.js` へ剥がし、
`plan.js` / `gap.js` は設定だけを渡す。差は2つ：

  ・**日にち**（第n曜日・最終週）は、きっかけだけが持つ
  ・**着手の記録**は、きっかけは枠ごと、すきまは枠に紐づけない
    （すきまの枠は「いつ」ではなく「どんな状況か」なので、
      枠ごとに数え分けると同じ着手が枠の数だけ増える）

**まだ手を付けていない**：`css/screens/gap.css` は前の格子レイアウトのままで、
いまの画面はどのルールも使っていない（`.gap-*` は DOM に無い）。消してよいが、
消すのは別の一手にしてある——読み込みは残っているので、消し忘れではない。

**【決着】色**：利用者の指示「すきまにいれたものも適当なカラーをつけて」で、
すきまの枠にも `hue` を持たせた。**色は3つから5つへ増やした**——
すきまの既定だけで4つあり、3色では足りなかったため。

色は総当たりで選んだ（明度も動かす。色覚特性のもとで効くのは色相より明度差なので）。
**5色にしても、いちばん見分けにくい組は足す前と同じ `#4fa3c7 / #6f6ab8` のままで、
最小色差（OKLab ΔE）は動かない**：明 2型 0.0986 / 暗 2型 0.0590（どちらも3色のときと同値）。
6色目まで足すと 0.0878 へ落ちたので、5色で止めてある。数値は `css/base.css` に。

**きっかけ側も一緒に変わる**（同じ `HUES` を使うので、4つ目・5つ目にも色が付く）。
色を持たずに保存されていたカードには、**読み込みのときに空いている色を配る**——
3色しか無かった頃の `null` を、増えたぶんへ渡すため。すきまの枠と同じ規則。

### A-52. 第三者レビューと、その返し（利用者の判断）

第三者に「思いつきや不安が次々に湧く人にとって、開始しやすい／継続しやすい／
習慣化しやすいか」を**批判的に**見てもらった。指摘は6つ。利用者の返答で、
**2つは事実誤認として却下**、2つは「いまは問題ない／別案で」、2つを実装した。

#### 却下1：「整理を頑張るほど起動画面が空になる」

> 中央の既定はタグ無しだけなので、仕事のタスクを始めるまで最短4〜5アクション

**利用者の返し**：どの海でも直接書けて、その場で始められる。今日からも書ける。

こちらで確かめた（コードと実画面）：タグの海の入力欄には
**「送信」と「書いて、すぐ始める（5分）」の2つ**が出る。書けばその海のタグが付く。
つまり「中央へ戻って、探して、運ぶ」という道は最初から要らない。
レビューは**中央を唯一の入口と見なして数えていた**が、入口は海の数だけある。

#### 却下2：「昨日の積み残しを今日へ運ぶのに1件4手」

**利用者の返し**：昨日の積み残しは既定の海に戻る仕様では。
完了も着手予定もないものは既定の海に居るはず。

そのとおりだった。実データで確認：`days:['昨日']` だけを持つ項目は、
`rollover()` の `syncTodayFlags()` で `today` が外れ、`tagsOf()` は **[]**、
未来の日も無い → **中央の海に出る**（A-47 の既定＝タグ無し・未来日なし に合致）。
運ぶ手作業は要らない。レビューは `days` が残ることだけを見て、
`today` が外れることを追っていなかった。

#### 保留：「きっかけに通知が無い」

**利用者の判断**：きっかけが習慣化の仕掛けそのもので、いまのところ問題ない。
通知は入れない。

#### 実装1：さがす（A-53 参照）

#### 実装2：長押しの手応え（A-53 参照）

#### 残っている宿題

- **「はじめた」の5分の閾値**（レビューの最重要指摘）。4分でやめると記録が残らない。
  `focus.js` の `reachedGoal = elapsedMs >= totalMs` を、海と今日の `onClose` が見ている。
  憲章に触れずに直せる（30秒で記録、など）。**未着手**
- **海の `markStarted` に番人とトーストが無い**（今日にはある）。二度押すとログが2件積まれる。**未着手**
- **「次の一手」が2タップの奥**。利用者から「1タップにする案は？」と問われている。**案を出すところ**

### A-53. さがす／長押しの手応え（レビューを受けて）

#### さがす

> 海は新しい20件まで、唯一の全件「ならべる」は古い順で絞り込みも文字検索も効かない。
> 書き出す人ほど、書いたものに二度と会えません → 「実装して」

置き場所は**「ならべる」の中**。あそこが「ぜんぶ読む場所」なので、さがすのもそこ。
新しい画面もタブも増やしていない。

- 対象は**本文・次の一手・リンク・積んだ記録**（`hay()`）。
  「あれ、なんて書いたっけ」は本文以外に書いてあることが多い
- **さがしている間は、ふだん外しているものも出す**——長期保留も完了も。
  探し物が見つからないほうが困る。代わりに行の右端に
  **いまどこに居るか**（長期保留／完了）を出す
- **件数は出さない**（§0）。無いときは「「◯◯」は見つからない。」だけ
- 見出しは「古い順」→「さがした結果（古い順）」に変わる
- 整列を畳むと、さがした文字も片づける（次に開いて前の結果が残っていると、
  「全部あるはず」が裏切られる）
- 入力欄は上に貼り付く。`.sea-grid` の上の余白を 60 → **112px** にした
  ——60 のままだと「もどす」ボタン（60〜104px）の下に潜って重なる（実測して直した）

確認：14件 → 「確定」で2件・見出しが変わる、「散歩」で長期保留のものが出て
右端に「長期保留」の札、「ざざざざ」で「見つからない」。
さがす欄（top 112）と もどす（bottom 104）は重ならない。

#### 長押しの手応え

> 長押しは円形のゲージがチャージされるエフェクトをつけて

指の下に輪を出して、貯まりきると開く。**貯まる途中で離せば何も起きない**ことも、
同じ絵で分かる。長押しは触って初めて存在が分かる操作で、しかも押している間ずっと
何も起きないので、効いているのか無視されているのかが分からなかった。

`ui.js` の `holdRing(x, y, ms)` に置いて、**2か所で同じ絵**を使う
（海の背景 480ms → 引き／今日タブ 450ms → 日の一覧）。同じ操作に違う手応えを出すと、
2つの別の仕掛けに見える。

- ドラッグ層（pointer-events:none）に置くので指は透ける。真ん中は mask で抜く
  ——指の下が見えないと、どこを押しているのか分からなくなる
- 角度は `@property --hold-p` で型を付けて補間（型が無いとパッと飛ぶ。bubble.css と同じ手）
- 「演出を減らす」設定では貯まる動きを出さず、**輪郭だけ**置く。
  動かないが「押しっぱなしを受け取っている」ことは伝わる

確認：押した瞬間に指の座標ちょうど（ずれ0）に出る／`hold-fill 0.48s`・`0.45s`／
貯まりきると開いて輪は消える／途中で離すと輪も消えて何も開かない／
なぞると即座に消える。**貯まっていく絵そのものは未確認**——この環境は
アニメーションを開始値で凍らせるので、静止画では空の輪しか写らない（契約 §14）。

### A-51.【直した】隣の海へ移れていなかった（名札だけが変わっていた）

> 「隣の海に移る動作が正常ではない」

そのとおりで、**世界が動いていなかった**。A-50 で面を横一列にしたとき、
落ち着き先を置く関数を直し忘れていた：

```js
function setWorldFace(face) {
  const p = { center: '0,0', up: '0,100%', down: '0,-100%',
              left: '100%,0', right: '-100%,0' }[face] || '0,0';   // ← 'sea:xx' は表に無い
  world.style.transform = 'translate(' + p + ')';
}
```

`'sea:<タグid>'` はこの対応表に無いので、既定の `'0,0'` に落ちる。
なぞっている最中は `faceBase()`（列番号から px を出す）で正しく付いてくるのに、
**指を離した瞬間に中央へ戻る**。`curFace` と名札だけが隣の海になり、
映っているのは中央のまま——という壊れ方だった。

列番号から割合で出すように直した（世界の幅はちょうど1列ぶんなので、
`-100% × 列番号` でその列が画面に来る。px ではなく割合にするのは、
ズームや回転で世界の大きさが変わっても測り直しが要らないため）。

**なぜ気づけなかったか。**A-50 の確認で見ていたのが**名札の文字**だったから。
名札は `curFace` から作るので、世界が動いていなくても正しく変わる。
**状態変数を見て、映っているものを見ていなかった。**

直したあとは、面の矩形そのもので測った（ステージの中心と面の中心のずれ）：

| 操作 | 名札 | 実際に映っている面 |
|---|---|---|
| 初期 | タグ無し | center（ずれ 0） |
| 左へなぞる | 買い物 | sea:買い物（ずれ 0） |
| もう一度 | 仕事 | sea:work（ずれ 0） |
| 右へ戻す | 買い物 | sea:買い物（ずれ 0） |
| もう一度 | タグ無し | center（ずれ 0） |
| 上の看板 | 長期保留 | up（ずれ 0） |
| 中央へ戻る | タグ無し | center（ずれ 0） |

測るときは遷移の規則を殺してある——この環境は遷移を開始値で凍らせるので
（契約 §14）、そのままだと**動いていないのか、凍っているのか**が区別できない。
最初の測定で「ずれ 30」が全場面で同じだったのがその印だった。

### A-50. 海を横一列にして10個まで／長押しで「引き」（利用者の指示）

> 「・タグ付き海の数を１０個まで作成できる様に
>  ・androidのデスクトップの様に、海の長押しで海の引きを見える様にし、
>   ＋ボタンで海の種類の追加ができる様に
>  質問して」

**訊いてから作った。**答えは4つ：横一列に並べる／端の帯は隣の海のタグが付く／
＋はその場で新しいタグを作る／引きでは並べ替え・海をやめる・名前と色を変える。

#### 並べ方（十字 → 横一列）

     　　　　  ┌────────┐
     　　　　  │ 長期保留 │        ← 上下は**中央の列にだけ**付く固有枠
     ┌────────┼────────┬────────┬─────┐
     │ タグ無し │  海1   │  海2   │ …  │  ← 横一列（10個まで）
     └────────┼────────┴────────┴─────┘
     　　　　  │  完了   │
     　　　　  └────────┘

- 面の名前は `'center' / 'up' / 'down' / 'sea:<タグid>'`。列の位置は CSS 変数 `--col`
- **器（div）はタグの数だけ作る。中身（drift の field）はいま居る列とその隣だけ。**
  10列ぶんのバブルを同時に持つと DOM が重い（1個で5ノード・7層・影1）
- **3枚を使い回す作り（前・いま・次）は採らなかった。**滑らせ終わったあとに
  中身を入れ替えることになり、drift はノードを id で使い回すので、
  入れ替えた瞬間にバブルが並び直る＝毎回ちらつく

#### store

`tag.dir` の左右2枠を、**順番を持つ配列**（`seas`）に置き換えた。`MAX_SEAS = 10`。
古い保存データは `dir` から左→右の順で移すので、見え方は変わらない。
`seas()` / `seaIndex()` / `isSea()` / `canBeSea()` / `addSea()` / `removeSea()` / `moveSea()`。
タグを消したら列からも降ろす。

#### 端の帯（利用者の選択：隣の海のタグが付く）

中央では 右＝最初の海／上＝長期保留。**左は出ない**——中央より左に海が無いため。
ここは正直に書いておく：**中央から一発でタグを付けられるのは、いちばん左の海だけ**になった。
遠い海のタグは、その海まで滑ってから端へ落とすか、盤のタグの札から付ける。

#### 引き（長押し）

背景を **480ms** 押したまま動かさなければ開く。指が 8px 動いたら、それはなぞり
（面の移動／寄せ）なので取り消す。バブルの上から始めた指はそもそもここへ来ない。

- 押すとその海へ入る／**掴んで並べ替え**／**‹ ›** でも動く（鍵盤だけでも届く）
- **⋮** で名前と色（色は store が配っているパレットから選ぶ。ここに別の色表を持たない）
- **海をやめる**（列から降ろすだけ。タグも中身も消えない。取り消しは**元の位置へ**返す）
- **＋** でその場に新しい海を作る（名前を書く。色は自動）
- **固有枠（長期保留・完了）も出す**。出さないと「上と下はどこへ行った」になる。
  ただし掴めず、×も出さない——押せない的を置かず、姿を分けて字で言う

#### 設定

左右の選び欄は意味を失ったので、**「海にする／している」**の1つに。
並べ替えは引きに置いた——順番はその場で見えているところで触るのが素直なので。

動かして確かめた：

| | 結果 |
|---|---|
| 面 | center / up / down / sea:work / sea:private（`--col` は 0,0,0,1,2） |
| なぞり | タグ無し → 仕事 → プライベート → 家、右端で止まる、戻れる |
| 10個 | 12個作って **10 で頭打ち**、引きは「10 / 10」＋＋が消えて「海は 10 個まで。」 |
| 端の帯 | 中央で 上＝長期保留・右＝仕事、左と下は出ない。落として『仕事』が付いた |
| 引き | 長押し 900ms で開く。札は ぜんぶ（いまここ）／海／＋／固有枠2つ |
| ＋ | 「買い物」を作って 3/10、列の末尾に並ぶ |
| ‹ › | 末尾を左へ → [仕事, 買い物, プライベート] |
| ⋮ | 名前を「おつかい」、色を #d2f5ca に |
| 海をやめる | 列から消え、タグは残る。取り消しで**元の位置**に戻る |
| 掴んで並べ替え | 先頭を3番目へ落として [プライベート, おつかい, 仕事] |
| 札を押す | 引きが閉じて、その海に入る |

明暗とも確認。6画面ともエラー無し、横スクロール 0、テスト 97 件通過。

**確かめられていないこと**：実機の指の長押し（合成の PointerEvent で測った）。
引きの掴んで並べ替えも合成の指。

### A-49. 今日の水面で、バブルを左右へ運ぶと日が変わる（利用者の指示）

> 「今日の海でバブルを左右にしたら、日付をその日に変更する機能」

掴んでいる間だけ、左右に帯を出す。そこで離すと、**いま映している日から外して
隣の日に置き直す**（足すのではなく動かす＝「変更」）。海の端の帯（.sea-edge）と
同じ作り方にしてあるので、覚えることは増えない。

**向きは画面に出ている ‹ › とそろえた。左＝前の日／右＝次の日。**
背景を右へなぞると前の日へ行く（紙をめくる向き）のと逆に見えるが、
なぞりは「面を動かす」、こちらは「バブルを置く」で、動かす対象が違う。
同じところに ‹ › が出ているので、**目で見える手がかりのほう**に合わせた。

**出さない場面が2つある。**

- **過去を映しているとき** … 過去はその日の記録。あとから書き換えない
  （`is-readonly` と同じ理由）
- **行き先が今日より前になるとき** … 盤のカレンダーで過去の枡を押させないのと同じ規則。
  今日を映しているときは**右の帯しか出ない**

札は帯の**上**に置いた。真ん中だと、掴んでいるバブル（指の下に来る）がそのまま隠す。
細い帯（62px）でも読めるよう、日と曜日を2行に分けて横書きのまま出している
（縦書きも試したが、バブルが重なったときに読めなかった）。
掴んでいる間は**さいころを消す**——右上 58〜106px は札の場所と重なるうえ、
さいころは落とし先ではない（海が掴んでいる間に看板を消すのと同じ）。

取り消しは、**掴む前の days をそのまま書き戻す**。足し引きを逆にたどるより確か
（複数の日に置いてあるものでも崩れない）。

動かして確かめた（今日 = 2026-09-01）：

| 場面 | 帯 | 落とした結果 |
|---|---|---|
| 今日を映す | 右だけ（9/2（水）） | days 9/1 → **9/2**、「9/2 へ」、取り消しで 9/1 に戻る |
| 明日を映す | 左（9/1）と右（9/3） | 左へ落として 9/2 → **9/1** |
| 昨日を映す | **どちらも出ない** | 端で離しても日付は変わらず、トーストも出ない |

タブへ落とす道（onDropToTab）とは二重にならない——`tabDropped` を立てて、
マイクロタスク1つぶん待ってから帯を見る（海・きっかけと同じ間合い）。
さいころは離すと戻る（opacity 0 → 1）。エラー無し、テスト 97 件通過。

### A-48. 絞り込みから「まだどこにも」を外した（利用者の指示）

> 「まだどこにもは消してOK ／ 今日のものは出さなくてOK」

A-47 で既定の眺めが「タグ無しだけ」になった時点で、あの行はほとんど同じものを
指していた（厳密には違う——あれは「今日・きっかけ・すきま のどれにも入っていない」で、
仕事だけ付いたものを含む）。**似て非なるものが2つ並んでいると、
「どちらを押せばいいのか」という問いが生まれるだけ**なので、外した。

絞り込みの一覧はこうなった：

    タグ無し ／ 今日より前 ／ 今日より後 ／ 今日 ／ きっかけ ／ すきま ／ 長期保留 ／ 仕事 ／ プライベート
    └ 押すと既定へ戻る    └ 日付           └ ここからタグ（store が持っている順）

あわせて、これで使い道の無くなったものを落とした：

- `isUnsorted()` … この判定の唯一の呼び先だった
- `NARROW_MARKS` … 同上
- `marksOf()` … バブルの点（追補4 §1 で廃止）のあと、絞り込みの判定だけに残っていた
- `isPlan()`（sea.js のローカル版）… marksOf からしか呼ばれていなかった
- `UNSORTED_ONLY` … 擬似 id

`NARROW_NAME` は「まだどこにも」→「しぼりこみ中」に変えて残した。
絞っているのに名前が引けなかったときの逃げで、ふつうは通らない。

動かして確かめた：一覧が9行（上のとおり）、「今日より前」→0件、
そこへ「きっかけ」を重ねて（または）7件、「タグ無し」で既定へ戻る、エラー無し、
テスト 97 件通過。

**「今日のものは出さなくてOK」**も確認できたので、A-47 の切り分け
（今日のものは今日の画面で見る）はこのまま確定にする。

### A-47. 中央の海の既定を「タグ無しだけ」にした（利用者の指示）

> 「まだデフォルト海に違和感があります。
>  ・今日より未来のタグ ・きっかけ ・すきま
>  がタグ付けされていれば、非表示（いつか見るので）
>  デフォルトの海はタグ無しのみ
>  フィルターは or にして、選択されているタグがあれば表示
>  今日より前・今日より明日のフィルターも追加」

A-46 は「出すもの／出さないもの」をタグごとに数え上げていた。数え上げは増えるし、
組み合わせ（今日＋きっかけ）のたびに判断が要る。**規則を1つにした**：

> **既定 … タグの付いていないものだけ**
> **絞ると … 選んだもののどれかが当てはまるもの（または）**

これで A-46 の `QUIET_TAGS` の場合分けは中央から消える。長期保留も特別扱いをやめて、
ただのタグに戻った（付いている＝既定では出ない／絞れば出る）。

**なぜ成り立つのか。**タグを持つものは、そのタグの置き場所に必ず出る——
今日は今日の画面、きっかけ／すきまはそれぞれの画面、長期保留は上の海、
仕事／プライベートは左右の海。だから中央から外しても、**どこにも出ないものは生まれない**。
中央は「まだ行き先の決まっていないものが漂うところ」になる。

**日付だけは、もう一段いる。**「今日より後」に置いたものは**タグを持たないことがある**
（days に未来の日が入っているだけ）。規則そのままだと既定に出てしまうが、
あれは「その日になったら出てくる」もの——利用者の言葉で「いつか見るので」——なので外す。

    既定 ＝ タグが1つも付いていない かつ 今日より後の日も付いていない

**「今日より前」は外していない。**やろうとして、やらなかったもの。海に居るのが正しい
（指示に挙がっていたのは未来だけなので、過去はそのまま）。見つける道として
絞り込みに「今日より前」を足した。

**絞り込みに足したのは2行**（「今日より明日」は「今日より後」と読んだ）：

| 行 | 中身 |
|---|---|
| 今日より前 | days に今日より前の日がある |
| 今日より後 | days に今日より後の日がある |

日付キーは `YYYY-MM-DD` の固定長なので、**文字列の大小がそのまま日の前後**になる（数に直さない）。

**名前を直した。**既定が「タグ無しだけ」を映すのに「ぜんぶ」と名乗るのは嘘になる。

- 面の名札（いま何が映っているか） … 「ぜんぶ」→ **「タグ無し」**
- 絞り込みの先頭（押すと既定へ戻る行） … 同じく **「タグ無し」**
- **行き先としての名前は「ぜんぶ」のまま**（矢印の看板・端の帯）。
  あそこは「その海へ行く」の話で、ならべる／しぼるでぜんぶに届く場所であることは変わらない

**空のときの言葉を3つに分けた。**既定を絞った以上、これが無いと「書いたのに消えた」に見える。

    まだ何も書いていない … はじめの案内（従来）
    絞った先に無い       … 戻り方
    **全部タグを持っている** … 「ここに漂うのは、タグの付いていないものだけ。
                              タグの付いたものは『▽しぼる』から、
                              ぜんぶは『⇅ならべる』から見える。」

**タグの海（左右・上）は A-46 の規則のまま**＋未来の日も外す
（その海そのもののタグは数えない）。

動かして確かめた（9通り作って測った。今日 = 2026-09-01）：

| 作ったもの | 既定の海 |
|---|---|
| タグ無し・日付なし | **出る** |
| タグ無し・**今日より前**の日 | **出る** |
| タグ無し・今日より後の日 | 出ない |
| 今日 / 仕事 / きっかけ / すきま / 長期保留 | 出ない |

- しぼる「今日より前」→ 過去のものだけ／「今日より後」→ 未来のもの2件
- 「今日より前」と「今日より後」を重ねる（または）→ 3件
- 「今日」→ 今日のものだけ／「きっかけ」→ きっかけのものだけ
- 「タグ無し」で戻すと、また既定の2件
- 仕事の海 … 仕事だけ（**仕事＋未来の日**は出ない）／長期保留の海 … 長期保留が出る
- 全部にタグを付けると、海が空になり**3つ目の言葉**が出る（案内は出ない）

**気になっていた2つは、どちらも答えをもらった**（「まだどこにもは消してOK／
今日のものは出さなくてOK」）。

1. 「まだどこにも」の行 … **外した**（下の A-48）
2. 「今日」を付けたものが海から消えること … **このままでよい**。
   今日のものは今日の画面で見る、という切り分けで確定。

### A-46. 海の既定から、きっかけ・すきま・長期保留を外した（利用者の指示）

> 「全ての海のデフォルトを、今日 + 未整理 + プライベート + 仕事 だけにして
>  正確に言えば以下三つが入っているものは除きたい（Not filter）
>  いまは or だと思うのですが、UIを複雑化せずに実装できないか検討
>  きっかけ／すきま／長期保留：毎日考えるものではない」

**UI は1つも増やしていない。**「または」の絞り込み（▽しぼる）はそのままで、
**既定で出さないタグが 1つ（長期保留）から 3つに増えた**だけ。
長期保留の扱い（A-34 以降「どの海にも既定では出さない」）を、そのまま横へ広げた形。

    const QUIET_TAGS = ['hold', 'plan', 'gap'];

3つに共通するのは、利用者の言うとおり**毎日は考えないもの**であること。
そしてどれも、ここではない置き場所を自分で持っている：

| タグ | 置き場所 |
|---|---|
| きっかけ | 「きっかけ」の画面（枠にぶら下がっている） |
| すきま | 「すきま」の画面 |
| 長期保留 | 上の海（自分で「いまは見ない」と決めたもの） |

だから海から外しても、行き場を失うものは無い。

**見に行く道は、もともと在るものがそのまま使える。**絞り込みの一覧には前から
全部のタグが並んでいるので、そこで「きっかけ」を選べば出る。
新しい札も、除外用の別のUIも要らない。

**その海そのもののタグは数えない。**長期保留の海が長期保留を出せなくなる、
きっかけを左右へ置いた人の海が空になる、といった事故を防ぐため
（`quietOf(t, except)` の except がそれ）。

**ならべる（一覧）はこの規則を継がない。**あそこは「ぜんぶ読む場所」で、既定の眺めではない。
既定で隠すものが増えたぶん、**全部を読める場所が1つ残っている**ことの価値は上がっている。
（長期保留だけは前から外してある——自分で「見ない」と決めたものなので。）

動かして確かめた（9通りの組み合わせを作って測った）：

| 作ったもの | 中央の海 |
|---|---|
| 未整理 / 今日 / 仕事 / プライベート | **出る**（4件とも） |
| きっかけ / すきま / 長期保留 | 出ない |
| **今日＋きっかけ** | **出ない** |
| **仕事＋すきま** | **出ない** |

- しぼるで「きっかけ」→ きっかけ と 今日＋きっかけ の2件が出る
- さらに「長期保留」を重ねる（または）→ 3件
- 「ぜんぶ」で戻すと、また既定の4件
- 仕事の海 … 仕事だけが出て、仕事＋すきまは出ない
- 長期保留の海 … 長期保留が出る（自分のタグは数えないので）
- ならべる … 完了と長期保留以外は全部（きっかけもすきまも出る）

**言っておくこと。今日＋きっかけ は、海から消える。**
「今日」は残したい4つに入っているが、「以下三つが入っているものは除く」を
そのまま採ったのでこうなる（Not が勝つ）。今日の画面には出るので迷子にはならないが、
**きっかけから今日へ持ってきたものが、海では見えない**という形になる。
「今日が付いていたら救う」に変えることもできる（1行）。指示の書き方が
「正確に言えば」だったので、まずは書かれたとおりにしてある。

### A-45. 下タブを4本にして、設定を右上の歯車へ（利用者の指示）

> 「・ひいてみるは削除
>  ・振り返りは設定の中に
>  ・設定は右上に歯車アイコンで別にして
>  ・海の上のフィルターや文字など、レイアウトを再考して」

**下タブは 6本 → 4本**（海・今日・きっかけ・すきま）。設定は右上の歯車、
ふりかえりはその設定の中へ移した。

この形には理屈が通っている：**下タブはバブルの落とし先でもある。**
受けない2本（ふりかえり・設定）が混ざっていたので、掴むたびに
「狙っても無駄なタブ」を薄くして断っていた（`data-drop="no"`）。
4本にすると**全部が落とし先**になり、断りが要らなくなる。
毎日触るのもこの4つで、設定とふりかえりはその頻度ではない。

**歯車は画面の中ではなく、アプリの枠（.app）の子**にした。どの画面から見ても
同じところに在る。押すと設定へ、設定（とふりかえり）に居るあいだは ✕ になり、
**さっきまで居た画面へ戻る**（戻り先を覚えている＝設定を見たあとに海へ落とされない）。
ドラッグ中は薄く（opacity .25）して触れなくする——落とし先は下タブだけ、という決まりを崩さないため。

**ふりかえりへは、設定のいちばん上の1行から。**設定の中身は「決める」もの、
ふりかえりは「見る」もの。混ぜずに分けて、行を1本置いて渡す。
ふりかえり側には「‹ 設定」を置いた——歯車（✕）は「設定ごと閉じる」で、
こちらは「ひとつ上へ戻る」。行き先が違うので、両方要る。

**引いて見るは削除。**倍率を動かす口はホイール（Mac の2本指スワイプ）と
つまむ指の2つになる。海の上に浮いていた札が1枚減った。

**海の上の並べ直し。**前は札が5枚（面の名前・しぼる・ならべる・引いて見る・さいころ）
浮いていて、右上に2段・左に2段が食い違って並んでいた。
引いて見るを外し、歯車が右上に入るので、**2段の左右**に組み直した：

| | 左 | 右 |
|---|---|---|
| 1段目（10〜54px） | 面の名前「ぜんぶ」 | **歯車**（アプリの枠） |
| 2段目（58〜104px） | ▽ しぼる | ⇅ ならべる ／ さいころ |

意味でも分かれる：**左は「いまどの海を見ているか」、右は「海に何をするか」**。
実測（375px 幅）、しぼるの右端 216px に対しならべるの左端 371px。重なりは無い。
上の看板（.sea-sign）は 118px → 112px へ上げた（さいころの下端が 110→104 になったので）。

**ほかの画面が歯車の下へ潜らないように**：

- きっかけ／すきま … `.plan` の上を 14px → **58px**（枠の右上に ⋮ があるので、潜らせない）
- 設定／ふりかえり … 上を **62px**
- 今日 … さいころを右上（10px）から **58px** へ下げ（海のさいころと同じ高さに揃えた）、
  日付の行の右端を `calc(100% - 74px)` で止めた

**全画面で当たりを測った**：歯車の矩形と重なる押せる要素は **0**（海・今日・きっかけ・
すきま・設定・ふりかえりの6画面）。移動の道筋も確かめた——
歯車→設定→ふりかえり→設定→✕→もとの画面、きっかけから開いて閉じるときっかけへ戻る。
テスト 97 件通過、エラー無し。

**残っている穴（申し送り）。ズームを動かす道がホイールとつまむ指だけになった。**
指1本しか使えない場面と、鍵盤だけで操作する人に、倍率を動かす道が無い
（前は「引いて見る」がその道だった）。要るなら、設定の中に倍率の段を置くか、
歯車の並びに小さく戻すか。指示が「削除」だったので、代わりは入れていない。

### A-44. 下タブに、その場所の色を出した（利用者の指示）

> 「下のタブの色を、現在の対応色にして（文字のみやすさに注意して）」

タブが指している場所の色を、そのままタブに出す。**いまの色**を出すので決め打ちせず、
`store.tags()` から読んで `--tab-c` に入れる（色は配り直しが起きうる。PAL_VER）。
配り直されたら追いかけるよう `store.on()` にも繋いだ。

| タブ | 色 |
|---|---|
| 海 | `--bub-edge`（海そのものの色。タグではない） |
| 今日 / きっかけ / すきま | そのタグの色 |
| ふりかえり / 設定 | **色を持たせない** |

ふりかえりと設定に色を付けなかったのは、**対応する色が無いから**。
揃えるために適当な色を割り当てると、「この色は何を指しているのか」に答えが無くなる。

**文字には載せていない**（指示の但し書き）。タグの色は12色すべて OKLCH の
L .855〜.935 の淡色で、白地に文字として置くと 4.5:1 どころか **2:1 も出ない**。
色を出すのはアイコンと、選んでいるタブの上の線・地の色だけ。ラベルは
`--text-2` / `--text` のまま**1ミリも動かしていない**。

アイコンも生の色では出さず、**文字色と混ぜて**沈めた（`color-mix(in oklab, var(--tab-c) 52%, var(--text))`）。
混ぜ先が `--text` なので、明るいテーマでは暗く・暗いテーマでは明るくなる——
**1つの式で両方のテーマに効く**。比率は 52%（選んでいるときは 66%）で、
アイコンが 3:1（文字ではない部品の下限）を切らないところまで色を寄せた。

実測（キャンバスに塗って sRGB に落として計算。`oklab()` のままでは比を出せない）：

| | 明・アイコン | 暗・アイコン | 明・文字 | 暗・文字 |
|---|---|---|---|---|
| 海（選択中） | 3.71 | 7.36 | 14.64 | 10.31 |
| 今日 | 4.62 | 11.71 | 5.85 | 6.79 |
| きっかけ | 3.83 | 13.64 | 5.85 | 6.79 |
| すきま | 4.30 | 8.88 | 5.85 | 6.79 |
| ふりかえり / 設定 | 5.85 | 6.79 | 5.85 | 6.79 |

アイコンはすべて 3:1 以上、文字はすべて 4.5:1 以上。上の線も同じ式なので同じ値。

**残っている問題（判断を仰ぎたい）。**明るいテーマで、**海とすきまのアイコンがほぼ同じ色**になる。

    海     rgb(101,131,143)
    すきま rgb( 99,126,138)

もとの色（`--bub-edge` の青と、すきまの空色 #a6e1fe）が近く、文字色と混ぜると
さらに寄るため。形（○ と △）と名前が違うので取り違えはしにくく、
色だけの手がかりにもしていない（1.4.1 は満たす）が、「色の見本」としては弱い。
直すなら (1) 海に別の色を割り当てる、(2) 海は色を持たせず選択中だけ色づける、
のどちらか。どちらも「海の色は何か」の答えが要るので、指示待ちにしてある。

### A-43. 書く口を「未分類」ひとつに寄せた（利用者の指示）

> 「きっかけの各枠にここにぶらさげるは無しで
>  未分類から作って載せる動線」

枠ごとにあった `[＋ ここにぶら下げる]` を外した。**書く口はいちばん下の「未分類」ひとつだけ**で、
そこから上の枠へドラッグして載せる。

**なぜこの順番が正しいのか。**書く口が枠の数だけあると、**枠を選んでから書く**ことになる。
けれどこの画面の枠は「きっかけ」＝すでに毎日かならず起きている行動で、
思いついたことのほうが先にあって、繋ぎ先はあとから決まる。前の作りは順番が逆だった。
（画面の下ほど「書けるところ」が増えていくのも、目のうるささとしてあった。）

**すきまにも同じことが起きる。**あの画面は同じ実装（cardlist.js）を軸だけ替えて使っており、
「すきまのUIをきっかけと基本的に同じUIに」という前の指示もあるので、揃えた。
指示の文面は「きっかけの各枠に」だけだったので、ここは判断を書いておく。

直したもの：

- 枠ごとの `[＋ ここにぶら下げる]` と、その場に開く入力欄を外した
- 未分類の入口を `[＋ ここに足す]` → **`[＋ ここに書く]`**（唯一の書く口になったので）
- 入力欄の言葉を「ここにぶら下げる」→ **「思いついたことを書く」**（海の入力欄と同じ言い方）
- **枠を足しても、その下に入力欄を開かない。**居場所は「きっかけを足す」の口に戻す
  （続けて枠を足せるように）
- 未分類の注記に「書くのはここだけ」を足した

動かして確かめた：

| | 結果 |
|---|---|
| きっかけの枠（3つ）／すきまの枠（4つ） | 入力の口は**0**。画面ぜんぶで1つ（未分類） |
| 未分類で書く | きっかけは付かず（`anchorsOf` が空）、この画面には残る（`plan` が真） |
| 未分類から枠へドラッグ | 「風呂から出たら」に付いた |
| 枠を足したあと | 入力欄は開かず、居場所は `addanchor`（足す口） |
| タグの札 | 今日・すきま・長期保留・仕事・プライベート（軸の「きっかけ」と「完了」は出さない） |

**残っている穴（指示待ち）。**枠へ載せる道が**ドラッグだけ**になった。
前は枠ごとの入力欄が鍵盤だけでも届く道になっていたので、ここは細くなっている。
バブルの盤にはタグの札があるが、**きっかけ／すきまの枠はタグではない**ので、
盤からは選べない。要るなら、盤に「どの枠へ」を足すのが素直（未指示なので入れていない）。

### A-42. タグを外す枠（利用者の指示）

> 「バブルをつまんだとき、真ん中したでタグを外すエリアを用意
>  0.5秒そのエリアにおいてから手をはなすとタグを外す
>  すべてのうみでは無し」

掴んでいる間だけ、**下の真ん中**に枠を出す。そこへ持っていって**少し置いてから離す**と、
いまの海のタグが外れる。**ぜんぶの海には出さない**——あそこは「すべて」で、
外す相手のタグが1つに決まらないため。

**なぜ「置いてから」なのか**を書いておく。端の帯（タグを付ける）は触れて離せばすぐ付くが、
外すほうは戻す手間が違う（付け直すには、その海まで運び直すことになる）。
だから、うっかり通り過ぎただけでは外れない。時間は指示どおり 0.5秒。

**0.5 という数字は画面に出さない。**代わりに、枠が左から満ちて、満ちたら
点線が実線に変わる。満ち具合が、そのまま残り時間になる（§0 の「数えない」に沿う。
色だけに頼らないよう、線の種類も一緒に変える——1.4.1）。

札は端の帯と同じ言い方にそろえた＝「いま離すとどうなるか」：

| いつ | 札 |
|---|---|
| 入ったばかり | ここに少し置くと『仕事』が外れる |
| 0.5秒たった | 離すと『仕事』が外れる |

**置き場所。**ステージの子（面の中ではない）にした。面に置くとズームの倍率が掛かって、
引いているときに狙いにくくなる。実測、倍率 0.45 でも 260×60 のまま、下から 16px。
左右の帯（厚み 21px）とは重ならない（375px 幅で両脇に 57px ずつ残る）。
指は透かす——判定は指先の座標でやるので当たり判定は要らず、持たせると
真下のバブルを掴めなくなる（A-8 と同じ理由）。

**上下の固有枠でも効く**（指示は「ぜんぶの海では無し」だけなので）。
外す先は `store.setTag(id, tagId, false)` に委ねてあるので、

- 長期保留 … 保留をやめる。**もどってくる日は一緒に落ちる**ので、
  取り消しに備えて外す前に控えて、戻すときに書き戻す
- 完了 … `uncomplete()`＝まだのほうへ戻る

動かして確かめた（すべて合成の指。契約 §14）：

| | 結果 |
|---|---|
| 仕事の海で 0.9秒置いて離す | 4件 → 3件、「『仕事』を外した」、取り消しで戻る |
| 0.15秒で離す | **何も起きない**（4件のまま、トーストも出ない） |
| 長期保留の海 | 2件 → 1件、「長期保留をやめた」、取り消しで戻る |
| 完了の海 | 完了 1 → 0・生きている 9 → 10、「まだのほうへ戻した」、取り消しで戻る |
| ぜんぶの海で掴む | **枠が出ない** |
| 引いた状態（0.45）で掴む | 枠は 260×60 のまま（倍率に引っ張られない） |

**確かめられていないこと**：満ちる動き（0.5秒のアニメーション）そのもの。
この環境は動きを開始値で凍らせるので、状態（`is-over` / `is-armed`）と
時間の効き目だけを測った。

なお、鍵盤だけで使う人の外す口は前からある（中央の盤のタグの札）。
この枠は指の近道で、唯一の口ではない。

### A-40. ズームを、本物のズームにした（利用者の指示）

> 「スクロールによるズームアウト、ズームインを実装してください。
>  引いてみるはバブルが小さくなってズームアウトとは別の概念になっている」
> （そのあと）「やっぱりズームアウトに合わせて海の幅も大きくして」

指摘のとおりだった。**前の「引いて見る」はバブルの直径だけを 0.8／0.6 倍にしていた。**
水面の広さはそのままなので、同じ面により多く収まる——これは「詰める」であって、
「遠くから見る」ではない。遠ざかれば、水面も枠も文字も一緒に小さくなる。

面ごと縮める本物のズームに替えた。ホイール（Mac の2本指スワイプもここに来る）と、
つまむ指と、右上のボタンの3つで動く。段は 0.4〜2.4、ボタンは 1 → 0.7 → 0.45 の3段。

**世界の広さは、一度「変えない」で組んでから「広げる」に変えた。**
最初に伺ったときは「世界の広さは変えない」を選ばれたので、そう作った。
そのとき正直に書いたとおり、**それだと引いても見えるものが増えない**
（端まで最初から見えているので、出てくるのは水の外側の余白だけ）。
そこを見て「やっぱり海の幅も大きくして」と返ってきた。いまは：

> **引いたぶんだけ、海そのものが広がる。** 世界の広さ ＝ 画面 ÷ 倍率。
> 0.45 まで引けば海は縦横 2.2倍＝**面積 4.9倍**。
> 画面に映る海の大きさは、どの段でも画面いっぱいのまま。

実測（15個、375相当の面で）：

| 倍率 | 世界 | バブルの半径 | 面積の割合 | 重なり |
|---|---|---|---|---|
| 1.00 | 430×588 | 51.5 | 65% | 34組 |
| 0.45 | 956×1307 | 23.2 | **13%** | **4組** |

しかも**画面の上での位置は動かない**（ずれの中央値 0px、最大 1px）。
その場で小さくなって、すきまが空く。戻せば戻る（戻したときのずれは最大 24px＝
狭まった壁に当たって押し戻されたぶん）。

1.00 より近づくときは世界を狭めない（狭めると、近づくほどバブルが押し込まれる）。
代わりに、はみ出したぶんを寄せ（pan）で見て回る。

**組み立て。**`.sea-stage > .sea-view（新設）> .sea-world > .sea-face×5`。

- `.sea-view` は**カメラの窓**。ステージいっぱいのまま動かず、はみ出しを clip するだけ
- 倍率と寄せは `.sea-world` に掛ける。広さ（width/height）もここに px で入れる

**窓のほうを縮めて一度しくじった。**世界が画面と同じ大きさのうちは同じ絵になるが、
世界が画面より広くなった途端に**窓が先に切り落とす**ので、広げたぶんが見えない。
引くと水が細い帯になった。倍率は「中身」に掛け、窓は動かさない。

**CSS の変形を3つの別々の口に分けたのが効いた**（合成の順は translate → scale → transform）：

| 口 | 使い道 | 値の単位 |
|---|---|---|
| `translate` | 寄せ | scale の**外**なので画面の px |
| `scale` | 倍率 | — |
| `transform` | 面の切り替え | scale の**中**なので世界の px |

3つは互いを上書きしない。おかげで**面の切り替えの式は1文字も変えなくてよかった**
（もともと世界の px で書いてあり、それがそのまま正しい単位になる）。
位置合わせ（世界を窓の真ん中に置く）だけは口が空いていないので**負の余白**を使った。
A-1 で transform 方式を見送った理由（座標系に手を入れることになる）は、
口を分けることで消えた。

**指の px と世界の px。**倍率が掛かると、この2つは倍率ぶんずれる。直したのは4か所：

- 面のスワイプ … 世界を置くときだけ `/ zLevel`（実測：指 250px → 世界 556px＝250/0.45）。
  しきい値（軸の決定・振り切ったか）は画面の px のまま——あれは「指がどれだけ動いたか」の話
- drift のドラッグ中／離したとき … **倍率は渡さない。**渡し忘れると静かにずれるので、
  drift 自身が `getBoundingClientRect().width / clientWidth` から出す
- 投げた勢い … 画面の px/秒を面の px/秒へ
- 位置は**左上＋半径ではなく中心**で渡す。中央の盤から離すときだけ、
  画面での大きさと世界での大きさが倍率ぶん食い違うため

**バブルの直径は、ステージの寸法で決めて drift へ渡す。**引くと世界は広がるが、
バブルは広がらない——それが「引いたぶんだけ余裕ができる」の中身。渡さないと、
drift は host（＝広がった面）の寸法で上限（`min(200, W*0.58, H*0.7)`）を決めるので、
画面の狭い機種では引くたびにバブルまで大きくなる。

**バブルを掴むと、倍率の掛かっていないドラッグ層へ移る**＝そのままだと大きさが跳ねる。
掴んだ時点の比を `scale` として掛け直し、原点を左上に置いて、動かすのは `translate` の口。
中央の盤を開くときだけ `scale` を 1 へ戻す＝**近づきながら開く**
（小さいまま開くと、盤は実寸なのにバブルだけ小さくて読めない）。

**端数の谷。**ホイールは掛け算なので、引いて戻すと 0.99996 のような端数が残る。
そのままだと「戻したのに枠が付いたまま・ボタンが〈もどす〉のまま」になった（実際になった）。
1.00 の近くは 1.00 に吸い付かせる。ただし吸い付いた値だけを持つと、細かく刻む
トラックパッドが谷から出られなくなるので、**刻みを足していく生の値と、実際に掛ける値を分けた**。

**測った値**（ステージ 430×588）：

- ホイールで指した点の**ずれ 0.25px / -0.05px**（近づくとき）。
  引くときは寄せ幅が 0 なので必ず中央へ寄る＝指した点は動く（世界が画面に収まるので、寄せようがない）
- 寄せの限界は `w*(z-1)/2`、`h*(z-1)/2` にぴったり一致（174 / 238）。**世界の外は決して見えない**
- つまむ指：2.0倍に広げて 2.035、0.5倍につぼめて 0.509
- 引いた状態でのドラッグ：持ち上げても大きさが跳ねない（46.7px のまま）、
  指との**ずれ 0 / 0**、離した先の**ずれ 0 / 0**
- 引いた状態で開く：実寸 104px・画面の中心 x=640 にぴったり
- 画面を離れると 1.00・寄せ 0 に戻る
- 6画面ともエラー無し、横スクロール 0、`store` のテスト 97 件通過、demo データも無事

**確かめられていないこと**（契約 §14）：

- **実機の指2本**。合成の PointerEvent でしか動かせていない。倍率の計算は確かめたが、感触は見ていない
- **つまむ指は、両方とも背景から始めたときだけ効く。**バブルの上から始めた指は
  bubble.js が掴んでいて、そこから取り上げる口が無い
- **遷移（ボタンで段を飛ぶときの 0.2 秒）**。この環境は遷移を開始値で凍らせるので、
  規則ごと殺して掛かる値そのものを測った。滑らかさは見ていない
- **近づいているとき、背景の指は寄せになる＝面のスワイプは効かない。**
  面を移りたければ矢印の看板を押す

### A-41.【直した】drift の「伸縮に合わせて位置を付け替える」が、一度も走っていなかった

ズームで世界を広げても中身が隅に残るので追ったところ、**ズームとは関係のない、
前からあった不具合**だった。`ResizeObserver` の中がこうなっていた：

```js
relayout();                        // ← この中で lastW = w; lastH = h; を書いている
if (lastW && lastH && (w !== lastW || h !== lastH)) {   // ← だから必ず false
```

`relayout()` は最後に `lastW / lastH` をいまの寸法で上書きする。そのあとで比べるので、
**条件は必ず「変わっていない」になる**。書いてあるコメント（「相対位置を保ったまま
伸縮させる（回転・分割表示でも散らばらない）」）は、一度も実行されていなかった。

前の広さを先に控えてから `relayout()` を呼ぶように直した。
副作用として、**画面の回転や分割表示でもバブルの相対位置が保たれる**ようになる
（これまでは物理の壁に押し戻されるだけだった）。

あわせて、この処理を `syncBounds()` として外へ出した。`ResizeObserver` は
「次の描画の前」に走る約束なので、描画が来ない状況（裏に回ったペイン、契約 §14）では遅れる。
ズームは1回のホイールで何度も広さを変えるので、遅れると中身が置いていかれる。
sea.js が広さを書き換えた直後に自分で呼ぶ。

### A-39. UI のレビュー（fable）に沿って直した

利用者の指示で、外の目にUIの洗練度を見てもらい、その指摘どおりに直した。

**いちばん重い指摘は、こちらの検証漏れだった。**
盤の中身が 671px で、見える高さ 622px を **49px 超えていた**——
[消す] が下タブの裏（top 741 / bottom 786、タブ 756）に隠れていた。
以前「収まっている」と報告したが、確かめたのは**盤の箱がタブバーより上か**だけで、
**中身が入りきっているか**を見ていなかった。カレンダーを足したときに越えている。

直したもの：

| 指摘 | 直し方 |
|---|---|
| 盤がはみ出し [消す] が画面外 | **いつの日**と**もどってくる日**を既定で畳む（触ると開く／タグの札と同じ方式）。畳んだ姿でも「いつの日か」は読める。実測 671→533px、はみ出し 0 |
| 入りきらないときの手がかりが無い | 入りきらないときだけ盤の下端をぼかす（mask なので押す的は塞がない） |
| 「今日は終わり｜OK」が [キャンセル｜OK] に見える | **OK →「書き留める」**。押したあとの「書き留めた」とそのまま繋がる |
| 3つのグレーが同一様式で重さが読めない | [タスク完了] の縁を一段濃く（暗 rgb(169,168,162) 対 rgba(255,255,255,.26)） |
| [履歴] が6個目のアクションに見える | 枠なし・左寄せ・12.5px の「履歴を読む ›」にして、要約している**作業メモのすぐ下**へ移した |
| 盤の文字が7段階 | 12.5 / 14 / 15 に畳んだ |
| 空のカードが埋まったカードと同じ高さ | 空なら升目を0行に。実測 245→**129px** |
| カードの「1件」バッジ | 消した。バブルは見えているので数字は情報を足していない（A-17 の「件数も出さない」と揃える） |
| ふりかえりの内訳 2×2 が割り算をさせる | 枠をやめ、縦に1行ずつの**文**に。数字はラベルの直後なので**列ができない**（実測 x=66 / 150 / 80 / 52） |
| 「アンカー無し」 | **「きっかけ無し」**へ（内部語 anchor の漏れ） |
| 設定だけ見出しがある | 外した。ほかの5画面に合わせる |
| 設定：組み込みタグ名だけグレーで無効に見える | `--text` に戻した |
| 設定：チェックボックスが粗い | 20→24px |
| 今日の ‹ › が 36 | **44×44**へ（幅の制約が無いので素直に直せる） |
| 文言の泣き別れ | 今日の空文を短く。作業メモの「記録できない」→「まだ積めない」 |
| 作業メモの案内が拒絶の形 | 「次の一手を書くと、ここから記録が積める」へ |
| 「今日するに入れた」 | 「今日の水面に置いた」へ |

**ついでに見つけた古い記述**：設定の案内が「海の上・左・右に置けるのは3つまで」のままだった
（上下が固有枠になった A-34 以降、事実と違う）。直した。

**直していない指摘**：カレンダーの枡 38×44（幾何的に 44 角にできない／A-35 に記録済み）。

実測（明暗とも）：盤のはみ出し 0、長期保留を出しても 0、[消す] は常に見える。
コントラストは 履歴を読む 6.79 / 書き留める 12.37 / タスク完了 12.37 / いつの日の要約 14.07。
6画面ともエラー無し、横スクロール 0。

### A-38. ぶつかったときのへこみ（利用者の指示）

> 壁やバブルがぶつかったときに少し凹んだりするモーションが欲しい

当たった向きに沿ってつぶし、指数で戻す。書く場所は `.bub` の `transform`——
`translate` の後ろに `rotate → scale → rotate` を足す。
**`.wob`（呼吸のゆらぎ）には書けない。**あそこは CSS アニメーションが transform を
持っていて、JS から書いても上書きされる。`.bub` の transform は drift の持ち物なので、
ここが唯一の空き channel。ふだん（へこみ 0）は文字列が今までと1バイトも変わらない。

強さ s = 0.09 × √(当たった速さ / 260)、1 で頭打ち。√にしたのは、漂っているだけの
当たり（相対 20〜30px/s）でも少しは動いてほしいから——線形だと 1% 未満で見えない。
　20px/s → 2.5% ／ 30 → 3.1% ／ 100 → 5.6% ／ 260以上 → 9%

**この動きも私の環境では観測できない**（rAF が発火しない／契約 §14）。
そこで重力のときと同じく **rAF と performance.now を差し替えて手で刻んで**測った：

  ・当たった瞬間の transform
    `translate(3.5px, 80.8px) rotate(-41.5deg) scale(0.986, 1.014) rotate(41.5deg)`
    ——当たった向き（-41.5°）に沿ってつぶれている
  ・128ms（8コマ）で 0.013 → 0.005 と指数で戻る
  ・当たらないまま刻むと 0 になり、transform は素の translate に戻る
  ・漂うだけの当たりで最大 2.4%（＝相対 18px/s 相当。式と合う）

**見た目そのものは実機でしか確かめられない。**9% が「膜が凹んだ」に見えるか、
それとも「楕円に化けた」に見えるかは、触ってみないと分からない。

### A-37. [OK] の役目を替え、作業メモを別の面へ（利用者の指示）

```
（バブル）
タグの札
いつの日（カレンダー）
もどってくる日（長期保留のときだけ）
次の一手  [        ]     ← 並びを入れ替えた（次の一手が先）
作業メモ  [ 書く ]       ← 入口だけ。押すと別の面
リンク    [        ]
[ タスク開始 ]
[ 今日は終わり ] [ OK ]  ← OK は いつの日・次の一手・リンク を書き留める
[ タスク完了 ]
──────
[ 消す ]
```

**[OK] は「いつの日・次の一手・リンク」を書き留める。**押しても盤は閉じない。
**閉じたときにも同じものが書かれる**——OK は「閉じずに、いま反映する」ための道であって、
押さないと消える道ではない（打った文字が消えるのは事故／追補2 §A と同じ考え）。

次の一手が変わったときは、記録として1件積む（`commitStep({did:'', next})`）。
A-23 で決めた「記録に触るのは押したときだけ」はそのまま。

**作業メモは盤に入力欄を持たない。**入口のボタンだけを置いて、押すと別の面が開く
（履歴と同じ入れ替え方。足すと下タブへもぐる）。面には
「次の一手：◯◯」を読むだけで添えてある——**何に続く記録なのかが要る**ため。
**[作業メモを保存] が記録を1件積む。**組むのは {作業メモ, いまの次の一手}。

入口のボタンには**いま書いてあるものの頭を出す**（16文字で丸める）。
押さないと分からない、にしない。空なら「書く」とだけ。

実測：次の一手を書いて OK → firstStep が入り、記録が1件。
作業メモの面で書いて保存 → 2件目が {メモ, 次の一手} で積まれ、欄は空になる。
閉じると入口に頭が出て、書きかけとして残る（記録にはならない）。

### A-36. きっかけ／すきまから「引力の井戸」を外した（利用者の指示）

利用者の報告：

> きっかけのバブル操作がおかしいので修正。規定位置での吸い込みなどを一度削除してよい
> もしかしたらバブルが画面裏に隠れているだけかも

**カードの中へ DOM で流し込む形に戻した。**物理は使わない。

**外した理由は、直せないからではなく、確かめられないから。**
引力・漂い・吸い込みは requestAnimationFrame の上で起きるが、
検証に使っているブラウザペインは裏に回っていて rAF が発火しない（契約 §14）。
**この機構は一度も観測できたことがない。**観測できないものを画面の中心に
置き続けるのは、直し方の当てが無いまま持ち続けることになる。

**「隠れているだけかも」も筋が通る。**面に置くと、セルから弾き出されたバブルは
重力で面のいちばん下まで落ちる。そこは最後のカードより下＝スクロールの外側で、
画面のどこにも出てこない。DOM なら必ずカードの中の `.grid` の子になるので、
落ちる先も隠れる先も無い。

**消したもの**（`js/screens/cardlist.js` から約180行）：
`fieldOn` / `resolveEntry` / `fieldHandlers` / `ensureField` / `dropField` /
`pushItems` / `nodeForKey` / `stampNodes` / `wellIdOf` / `cellCenter` /
`applyWells` / `fieldItemFor` と、呼び出し側の枝。
`bubblesOfAnchor`（並べ替えでバブルを一緒に運ぶ）は、カードの `.grid` を引くだけになった
——バブルがカードの子になったので、そもそも自分で運ぶ必要がほとんど無い。

**`drift.js` の `setWells` / `wellOf` は残してある。**海と今日は drift をそのまま使うし、
井戸の実装そのものは壊れていない（呼ぶ人が居なくなっただけ）。
**呼び手が1人も居ない状態なので、そこは次に整理する候補。**

実測（すべて DOM 配置で）：きっかけ・すきまとも中身が正しいカードに入る／
面に直接置かれたバブルは0／カード間のドラッグ・未分類への出し入れ・タップで盤／
**カードを並べ替えると中のバブルも一緒に動く**（子になったので自動）。

### A-35. 日の移り方に手がかりを付け、日をカレンダーで入り切りできるように（利用者の指示）

**左右になぞって日を移るのは、前から動いていた**（A-17 の回）。
実測でも 左へなぞる→明日 / 右へ→今日 と動く。**書いていなかっただけ。**
海には矢印の看板があるのに、ここには手がかりが無かった。

日付の札の両脇に **‹ ›** を置いた。**押しても移れる**（指だけの経路にしない／A-9 と同じ）。
36×36 で、真ん中の札（36px）と並ぶ。

**日を入り切りするカレンダー**を盤に足した（利用者の指示）：

> todayタグのもののdaysをカレンダーでタップしてON/OFF出来るようにしたい
> （今日はできなそうなので、9/3,4に移す、みたいな）

最初は「この日から [前の日へ][次の日へ]」で組んだが、**どの日の話かを画面の文脈に
頼っていた**（今日の画面でしか出せなかった）。カレンダーは日そのものを見せるので、
**どの画面の盤からでも使える**。前の形は置き換えた。

  ・**3週間ぶん**（今週の日曜から）。遠い過去を一覧にすると、それ自体が
    「やらなかった日」を数える装置になる（§0。日を選ぶ札と同じ考え）
  ・**過去の枡は押せない。**過去はその日の記録なので、あとから足させない・消させない
    （入力欄を過去に出さないのと同じ規則）。**印は出す**——置いた事実は読めたほうがいい
  ・**書くのは閉じるとき。**タグの札と同じ（押すたびに書くと店が emit して盤が閉じる）
  ・**「今日」の札とカレンダーの今日の枡は同じもの。**書き込みはカレンダーが持ち、
    札は見た目だけ合わせる（`saveTags` は today を書かない）——二重に書かないため

**枡は 44px 角にできない。**盤の中身は 300px（min(320,100vw-24) − padding 20）で、
7列に 44px を敷くと 7×44 + 6×2 = 320 > 300 で入らない。
**高さは 44 を保ち、横は 40**（7×40 + 6×2 = 292）。実測 38×44。
契約 §15 の 44×44 から外れる**幾何的にやむを得ない例外**として記録しておく。

実測：9/3・9/4 を押すと印が付き、閉じると days が
['2026-08-30','2026-09-03','2026-09-04'] になる。今日を外すと今日の水面から消え、
9/3 の水面に出る。過去（8/30）は押しても変わらない。8/30 のぶんは触られない。

### A-34. [タスク開始] を盤の中へ。「はじめた」を「今日は終わり」へ（利用者の指示）

**① 導線が弱くなっていた。**[まずは開始] は追補3 §5 で「いちばん上」と決めて
バブルの**上**に浮かせていた。バブルを画面の上部へ移してから（A-31 の前の回）、
そこが画面の縁ぎりぎりの細い帯になり、**いちばん強いはずの導線がいちばん見つけにくく**なっていた。
利用者の指示で、**盤の中の「今日は終わり」の上**へ移した。塗りつぶしのまま、48px。
バブルの上に空けていた 56px も要らなくなったので、そのぶん盤に使える高さが増えている。

**② 「はじめた」と「まずは開始」の違いが読めなかった。**
どちらも「開始」と読めるのに、片方は5分の集中画面へ進む道、
片方は着手の印（押すとバブルが薄くなり、5時に戻る／契約 §5）だった。
利用者の指示で名前を替えた：

  まずは開始 → **タスク開始**
  はじめた   → **今日は終わり**

**記録している中身は変えていない**（`store.start` の着手印のまま）。
名前を変えたのは、この印が実際にしていることが「今日ぶんはここまで」だから。

**同じ操作を指していた言葉が3つあった**（「はじめた」「開始した」「今日は終わり」）ので、
**操作の名前は全部そろえた**：盤のボタン／長押しメニュー／詳細シートのトグル（3画面）／
一覧の印／トースト／読み上げ文。ついでに長押しメニューの「完了」も
盤と同じ「タスク完了」に（となりに「今日は終わり」が並ぶので、
どちらが重いほうかが名前で読めないと取り違える）。

**そろえなかったのは記録の名前。**ふりかえりの内訳と、きっかけのカードの数え上げは
「はじめた」のまま。あちらは操作ではなく**記録**の名前で、README の憲章が
「記録するのは『はじめた』だけ」という言葉を使っている。**ここは指示待ち。**

実測：盤は タスク開始(48px) / 今日は終わり(44) / OK(44) / タスク完了(44) / 消す(44)。
[タスク開始] の白文字は --accent-a に対し 4.70:1（前と同じ）。
6画面とも読み込みでエラー無し。

### A-33.【解決】バブルのタップが、時々そのまま完了になっていた（利用者の報告）

**再現した。**タップは `pointerup` で中央を開き、**盤はその場で組まれる**。
ブラウザが `pointerup` のあとに出す `click` は、**盤が並んだあとの座標で当たりを取る**ので、
離した点にちょうど盤のボタンが来ていれば、その `click` がボタンに入る。

実測：(90,500) で離すと、`pointerdown` の時点では `.skin`、20ms 後には `.bc-act-done`。
そのまま `click` が入り、「保存した記事を読む」が完了になった（トーストまで出た）。

**起きやすさを上げたのはこちらの変更。**バブルを上部へ移したとき（利用者の指示）、
盤が 180〜719px を占めるようになり、当たる面積がそれまでの倍以上になっている。
「時々」なのは、離した点に何が来るかが、バブルの居場所で決まるため。

**直し方**：開いたタップが生む `click` を**1つだけ**捨てる。絞り方は2つとも要る。
  ・時間 … 開いてから 400ms 以内（compat click は数 ms で来る）
  ・場所 … 離した点から 12px 以内
どちらかを外れたら人が狙って押した `click` なので通す。
キーボード（Enter / Space）で開いたときは `click` が生まれないので、そもそも張らない。

実測：離した点に `.bc-act-done` / `.bc-act-started` / `.bc-act-ok` のどれが来ても状態は動かない。
40ms 後でも**離れた場所のボタンは押せる**（OK が「次の一手が要る」と応えた）。
500ms 後は同じ点でも押せる。盤は開いたまま。

**長押しの経路は無事**だった。海でもきっかけでも、長押しは盤（中央）を開く道になっていて、
離した点には覆い（`.bub-veil`）が来る。覆いは `click` を食うので、もともと素通りしない。

### A-32. 集中画面から履歴を読めるようにした（利用者の指示）

追補2 §E は「**履歴をたどる画面は作らない**」だったが、
利用者の指示で盤に [履歴] を作り（A-24）、今回その口を集中画面にも足した。
**追補の当該項目は、利用者の指示で覆されている。**

**一覧を組む所は1つにした。**`bubble.js` から `stepList()` / `fillStepList()` を出して、
盤と集中画面が同じものを使う。別々に組むと、片方だけ直したときに
同じ記録が2つの顔で出る（詳細パネルが4実装ある件と同じ轍。B-2）。
スタイル（`.bh-*`）は `css/bubble.css` にあり全画面で読まれるので、CSS の追加は不要だった。

**置き場所は「記録する」の下。**最初は「最後になにをしてたか」の隣に置いたが、
開いた瞬間に 次の一手 と 記録する が画面の外へ出た（実測。高さを 240px に詰めても足りない）。
**この画面の仕事は書くほうで、履歴は読むための添えもの**なので、添えもののほうが下がる。
実測：開いても「記録する」の位置は 1px も動かない（top 669 のまま）。

記録が1件も無ければボタンごと出さない。積むとその場で出て、開いたまま増える（実測 2→3件）。
タイマーには触っていない（追補2 §A）。

### A-31. 海に表示上限を付け、「ならべる」を文字の一覧にした（利用者の指示）

**海は新しいほうから 20 件まで。**隠れたぶんは「ならべる」で全部読める。

**数は 50 で組んで、測って 20 に下げた。**指示は「50個など」だったので 50 を試したが、
面はスクロールしない1画面なので、入らないぶんはそのまま重なりになる：

| 出す数 | バブルの面積の合計／面 | 1個あたりの重なり相手 |
|---|---|---|
| 50 | **220%** | 8.1個 |
| 20 | 88% | 3.4個 |
| 20（0.8倍） | 57% | 2.1個 |
| 20（0.6倍） | 32% | 1.0個 |

50 は文字が読めなかった（実測。A-1 の「17個で 73%」と地続き）。
20 なら等倍でも読め、「引いて見る」で余裕が出る。**実機で触れたら決め直すこと。**

**隠すのは古いほう。**古くて手つかずのものこそ「やっていないことの山」になるので、
漂わせ続けないほうが憲章に合う。

**「ならべる」は文字の一覧に作り替えた。**海が全部を出さなくなったので、
ここが「ぜんぶ読む場所」になる。読む場所は絵より文字のほうが速い。
バブル1個は DOM 5ノード＋グラデーション7層＋影1枚＋動くCSSアニメ1本で、
行なら3ノードで済む。押すと長押しメニュー（詳細／5分／はじめた／完了／消す）が開く
——一覧のために別の操作を発明しない。

**見出しを「古い順 — 放置しているものが先頭」から「古い順」に変えた。**
出す範囲が広がったぶん、あの札は未達の名指しに寄りすぎる（§0）。
並びは古い順のまま＝海から隠れたものが先頭に来る（探しに来た人が最初に見る場所）。

**出し切れていないときの1行**は「新しいほうから出している。「ならべる」で、ぜんぶ。」。
**数は出さない**——「あと◯件」は、やっていないことの数を突き返すのと同じになる。
バブルの上に重なるので地を敷いた（敷かないと読めなかった。実測）。

**まだ手が付いていない**：一覧は描画のたびに全行を作り直す。
75件で 67.8ms（この Mac、強制レイアウト込み）。開いたときだけ走るので今は許容だが、
数百件になったら作り直しの範囲を絞ること。

### A-4.【保留】保存容量の肥大化（B-4）

利用者の判断：**「まだプロトタイプなので保留」。**
`steps` / 完了 / 墓石 / `log` のどれも間引かない。保存に失敗したときに
それを画面へ出す道（`onSaveError`）だけが入っている状態のまま。

測った値は B-4 に残してある（1日 2,758文字の増え方で 5MB まで約5.2年、
引っかかりは 3MB＝約3年から。iOS で消される話は間引きでは防げない）。

### A-29.【解決】きっかけのバブルの挙動が不自然（利用者の指摘）

> きっかけのバブルの挙動が不自然。ドラッグに正しく追随しない

**追随そのものは合っていた。**実測すると、指先とバブルの中心のずれは掴んだときの
ずれのまま（0px の誤差で追いかける）。スクロールしていても、端を掴んでいても同じ。
不自然に見えていたのは、**離したあと**に起きる2つのことだった。

**① 落とし先を指先だけで見ていた。**
カードは 375 幅の画面に 288 幅で置いてあり、左右に 44px ずつ余白がある。
バブル（96px）の端のほうを掴むと指先は中心から 34px ずれるので、
**バブルはカードの上に乗って見えているのに、指先は余白の外**、という状態が作れる。
そこで離すと、見た目に反して未分類へ落ちる（トーストは出るが、動いた理由が読めない）。

指先で見て、外れたらバブルの中心で見直す形にした。契約 §14 の
「ドロップ判定は指先の座標で」は**タブバー**のための決まりで、理由は
「バブルの外形はタブ6本ぶんを覆う」から。カードは 288×245 あってバブルより
ずっと大きいので、その心配はここには無い。指先を先に見るのは変えていない。

**落としたときの中心は、動いている間に控えておく。**離したあとの判定は
マイクロタスク1つぶん遅れて走り、そのときにはノードがドラッグ層から戻っているので、
その場で測ると「戻った先」の位置が出てしまう（最初この形で書いて、
光る枠は正しいのに落ちる先だけ間違う、という状態になった）。

**② 掴んでいる間、面が送れなかった。**
カードの並べ替えには端での送りが入っていたが、**バブルのドラッグには無かった**。
カードは1枚 245px で画面には3枚ぶんしか入らないので、
いま見えていないカードへは**そもそも運べなかった**——途中で離すしかなく、
離せば未分類へ落ちる。運べないのに運ぼうとする、が不自然さのもう半分。

実測：バブルの端を掴んで指先をカードの外へ出して離しても、元のカードに残る。
下端へ運ぶと面が送られ、画面の外にあったカードへ落とせる（離すと送りは止まる）。

### A-27. しぼりこみをタグごとに（→ A-30 で複数選択へ）

利用者の指示。前は「しぼる」⇄「もどす」の2択トグル（＝まだどこにも入れていないもの）
だった。いまはボタンの真下に一覧が出て、**ぜんぶ / まだどこにも / タグそれぞれ**から選ぶ。

**なぜ要るか**：向きは左右の2つしか無く、上下は固有枠なので、
ユーザーのタグは3つ目から「海が無い」状態になる。しぼりこみがその見に行く道になる。
長期保留も、ふだんはどの海にも出ないが、ここから呼べば出る（実測ずみ）。

**完了は一覧に出さない。**下の海とふりかえりに専用の道がある。
戻し方は一覧の先頭（ぜんぶ）に置いた——別のボタンにすると、
絞っている間だけ現れる的が増える。

**効くのは中央の海だけ**（前からの決まり）。タグの海には絞る意味が無いので、
ボタンごと出さない。

### A-30. しぼりこみを複数選択に（利用者の指示）

**重ねると「または」**（選んだ種類のどれかが付いているもの）。

**最初は「かつ」で組んだ。これは間違いだった**（利用者の報告「フィルターがうまく機能していない」）。
「絞る」という言葉から素直に考えたが、**言葉から考えて、データを見ていなかった**。
このアプリでは1件が持つ置き場所のタグはふつう1つなので、
**2つ選ぶと必ず0件になる**——実測で、組み合わせ **15通りすべてが0件**だった。
それでは重ねる意味が無い。**組む前に1回数えれば分かったこと。**

「または」でも絞り込みではある。「ぜんぶ」から「この種類だけ」へ狭めている——
狭め方が「全部を同時に持つもの」ではなく「どれかを持つもの」というだけ。
実測：きっかけ 5 / ＋すきま 9 / ＋仕事 10 / すきまを外すと 6 / ＋まだどこにも 11。
どれも store の数と一致し、使える数になる。

  ・「ぜんぶ」は一覧の先頭に置いた**選択の取り消し**で、トグルではない
    （押すと閉じる。ほかの行は重ねられるので、選んでも閉じない）
  ・札とボタンの文字は、2つ以上のとき「◯◯ と 2つ」——全部並べると札からはみ出す
  ・設定でタグを消したら、選んだままにしない（`pruneNarrow`）

絞りは**中央の海だけ**に効く。タグの海へ移るとボタンごと引っ込み、戻ると絞りは残っている。
「ならべる」には掛けない（契約 §7「全件が並ぶ」。追補4 §2 が「決めて報告に書くこと」
としていた点で、こちらに決めてある）。実測：絞って 4件 → ならべるは 15行 → もどすと 4件。
0件のときの言葉も直した——「もどす」というボタンはもう無いので、
「しぼりこみで『ぜんぶ』を選ぶと」に書き換えてある（画面に無いボタンの名前を書かない）。

### A-21.【要確認】もどってくる日を見にいく合図が、時計ではなく「見る直前」

`sweepHolds()` を呼ぶのは3か所。**どれも人が画面を見る直前**で、時計では起こさない。

  ・起動したとき（最初の描画より前）
  ・画面を切り替えたとき（`app.js` の `show()`）
  ・アプリが表に戻ったとき（`visibilitychange`）

タイマーで起こさないのは2つの理由：見ていない画面のバブルが独りでに増えても
誰も得をしないこと、裏に回っていると `setTimeout` は間引かれて当てにならないこと（§14）。

**代償**：海の画面を開きっぱなしで朝5時をまたぐと、タブを触るまで戻ってこない。
実測では、日をまたいだあと最初のタブ切り替えで戻る。
気になるなら海の `onShow` にも足せるが、そこは既に `show()` が通る道なので
効くのは「同じタブを押し直したとき」だけになる。

**知らせ方**：1件なら名前、複数なら「◯◯ ほかN件が海にもどった」。
取り消しボタンは付けていない——自分で決めた日が来ただけで、取り消す対象の操作が無い。

### A-22.【決着】長期保留の日は、盤を開かないと見えない

「もどってくる日」が見えるのは、そのバブルの盤を開いたときだけ。
上の海は**日が近い順**に並ぶ（`store.holds()`）が、日そのものはどこにも出ていない。
利用者の判断：**「このままでOK」。**変更なし。

### A-3.【決着】完了が0件のとき「0件 完了」と出る

利用者の判断：**「それでよい」。**変更なし。

---

## B. 直したいが、指示待ちのもの

### B-1.【解決】タグ無しバブルの暗テーマのコントラスト

作り直した走査（幾何入り）で **2.27:1**（前の記録 2.48 とほぼ同じ）。
照りの点（`.skin::before`）と照りの radial が重なる (31%, 20%) あたりで
`--bub-text` #d8eef8 が沈む。パレットの差し替えより前からある。

**直し方に選択肢が2つあり、代償の小さいほうを採った。**

- 却下：照りと虹色を薄くする。4.5 に届かせるには**全体を 0.40倍**まで
  落とす必要があった（glow .28→.11、irid .20→.08。膜がほぼ平らになる）。
  照りだけ 0 にしても 3.72 までしか上がらない——虹色3層が別の最悪点を作るため
- 採用：**タグの膜とまったく同じ置き方・同じマスクで、色だけ地に寄せた1枚を掛ける**
  （暗テーマのみ）。染めたバブルが 5.04 で通っている理由をそのまま持ってくる。
  色 = `color-mix(in srgb, #9fd6f8 20%, var(--bg))` = rgb(49,60,70)、濃さ .65

実測 **未着手 5.15:1 / dim 7.36:1**（濃さ .60 で 4.80、.55 で 4.49 だったので .65 を採った）。
明テーマは veil 無しで 6.84 通っているので掛けていない。

代償：暗テーマのタグ無しバブルは虹色の照りが落ち着く。ただし**染めたバブルは
前から同じくらい落ち着いていた**（あちらは濃さ .88）ので、暗テーマの見た目はむしろ揃う。
気に入らなければ `css/bubble.css` の `.bub:not(.is-tinted) .tint` を消すだけで元に戻る。

### B-2.【見送りを推奨】詳細パネルが4実装ある

`sea.js` / `today.js` / `plan.js` / `gap.js` がそれぞれ自前で持っている。
共通部品（`js/detail.js`）に括るべきだが、契約に入れ忘れたまま来ている。

**いまはやらないことを推奨する。**画面から見える利得がゼロで、
4画面を同時に触る回帰リスクだけがある。しかもこの4つは
「分担して別々に書いた」結果そうなっているので、細かい挙動の差が
どこにどれだけあるかを先に洗い出さないと、括った瞬間にどれかが変わる。

やるなら、**次に詳細パネルの仕様そのものを変える指示が出たとき**に
一緒にやるのが安い（どうせ4か所とも触るため）。

### B-5.【解決】海のヒント文の揺れ・背景の光・粒の上昇が reduce で止まらない

`css/screens/sea.css` の `reduce` ブロックに
`.sea-stage::before, .sea-stage::after, .sea-hint { animation: none !important; }` を追加。
止めるだけで絵は残す（背景の光も粒もヒント文も、静止した状態で見える）。
`spark-rise` は translateY で継ぎ目を作っているが、切ると初期位置＝継ぎ目の無い状態で止まる。

### B-3.【解決】`sound.js` の import が不統一

`today.js` / `gap.js` を静的に寄せた（`sea.js` / `plan.js` / `settings.js` と同じ）。

動的にしてあった理由はコメントに書かれていた——「sound.js が無い／壊れている間
この画面ごと読めなくなる」。**その守りはもう効いていない。**
他の3ファイルが静的に読んでいるので、sound.js が壊れればどのみち立ち上がらない。
残っていたのは不揃いと、**最初の1回だけ音が遅れる**副作用だけだった
（モジュールの解決を待つため。音は操作に添うものなので、そこが遅れるのがいちばん困る）。

### B-6.【解決】パレット差し替え後の、染めたバブルの文字コントラスト

走査の道具を作り直した（`<scratchpad>/pal4/geo.mjs`）。幾何を入れてある：
radial の farthest-corner、linear 125deg の線の長さ、`::before` の楕円と rotate(-28deg)、
inset box-shadow のぼかし、`.tint` のマスク。
文字box（直径の 15〜85%）を 15×15 点、直径 78/96/200、面の背景6種 ×（光あり／なし）、
染めていない側は hue-rotate ±22deg（filter は文字にも掛かるので文字色も回す）。

道具の妥当性：前の条件を測ると 染めていない 明 6.84（前の記録 6.86）／暗 2.27（同 2.48）。
**わずかに厳しく出るが、ほぼ同じものを測れている。**

| | 未着手 | dim | mark |
|---|---|---|---|
| 明 染めた | **7.00** | 7.50 | 7.00 |
| 暗 染めた | **5.04** | 5.49 | 5.54 |
| 明 染めてない | 6.84 | 7.78 | — |
| 暗 染めてない | **5.15**（veil 後・前は 2.27） | 7.36 | — |

**どれも 4.5 を割らない。**外挿していた「5.2 前後」に対して実測 5.04。
青竹 `#adfde1` を暗くする案（前回の (a)）は**不要になった**。

### B-4.【半分だけ解決】`steps` と `done` に上限が無い

**やったこと：`persist()` が黙らなくなった。**
`store.onSaveError(fn)` / `store.saveError()` を足し、`app.js` が
「いま書いたものを保存できなかった。端末の空きが足りないかもしれない」を出す
（同じことは繰り返さない。次に保存が通るまで黙る）。テストあり。

前は握り潰していたので、**書いたものが保存されていないのに画面はふつうに動き続け、
次に開いたときだけ消えている**という壊れ方をした。そこは塞いだ。

**やっていないこと：上限を作って古い記録を間引くこと。**
記録を捨てるのは「記録として残し続ける」という前提に触るので、勝手には決めない。
決めてもらいたいのは3つ：

1. 間引いてよいか。よければ何を（`steps` / 完了の海 / 墓石）
2. 残す数（例：一手の記録は項目ごとに直近50件、完了の海は直近365日）
3. 間引いたことを利用者に見せるか、黙ってやるか

---

## C. 確かめられていないこと（環境の制約）

ブラウザペインが `visibilityState: hidden` のことが多く、`requestAnimationFrame` が発火せず、
`setTimeout` が1秒単位に間引かれる。そのため次は**一度も目視できていない**。

- バブルの漂い・呼吸・飛沫・中央へ寄る動き
- **弱い重力の落下**（raf を差し替えた数値検証は済み: 終端 11px/s、上端から底まで 49〜60秒、
  跳ね返りゼロ、面外への逸脱ゼロ、`reduce` で不動）
- **タグの多色の移り変わり**
- **完了音**（`playComplete()` が例外なく呼ばれるところまで。音そのものは聞いていない）
- `prefers-reduced-motion: reduce` の実挙動（コードと CSSOM の確認まで）
- **実機タッチ全般。**すべてマウスの合成イベントでの検証

---

## D. 決めたが、根拠が弱いので覆してよいもの

| 決めたこと | 理由 | 覆すときの手間 |
|---|---|---|
| 完了の記録をログに積まない | 積むとふりかえりが達成率の絵になる | store に系列を1つ足す |
| ふりかえりの帯を廃止（数字だけ） | 帯は共通目盛りで、%を出さなくても比率の絵になる | `review.js` を戻す |
| 0件のきっかけは行ごと出さない | 空の帯が並ぶのは未達の名指しに近い | `.filter()` 1行 |
| 画面3の「先頭が主役」を廃止 | 枠の中は一定サイズ、を優先 | `plan.js` に例外を戻す |
| 設定を6つ目のタブとして残した | 音とタグの置き場が要る | タブを減らして別の場所へ |
| タブの記号を ○ ◎ ◇ △ ◔ ⚙ に | ◐と◑、◔と◔ が 17px で見分けられなかった | 各画面の `icon:` 1行 |
| 中央の URL 欄は、不正なとき縁の色だけで知らせる | 叱らない書き方に寄せた | 文言を足す |
| Cmd+Enter の扱い・時計ボタンの見た目 | （担当が決めた内容は完了報告を参照） | — |
| きっかけを 白橡 → 卵色 `#ffe8a4` | 今日との色差 0.0764→0.1001、パレット全体の最小 0.0445→0.0672。緑へ移す案は両方とも下回った | `TAG_DEFAULTS` 1行＋`PAL_VER` |
| 多タグの巡回を 0.7倍に速めた | 利用者の指示。拭き替え・縁の分割は入れないとの判断 | `bubble.css` の 14s / 21s |
| 「消す」に確認を1枚入れた | UI から戻す道が無い（墓石は内部だけ）。トーストは数秒で消える | `bubble.js` の `askDelete` を外す |

---

## E. 退避

このリポジトリは **git 管理外**（`.gitignore` の `project_dev/apps/*`）。
着手前の状態は次に退避してある。

```
<scratchpad>/bubble_todo_backup_0112/
```

契約（作業を分担するときに全員へ渡したもの）:

```
<scratchpad>/CONTRACT.md    最初の作り替え（6タブ・バブル部品・データ層）
<scratchpad>/ADDENDUM.md    集中画面の作業ログ・中央からの復帰
<scratchpad>/ADDENDUM3.md   タグの海・重力・完了の海・中央の作り替え
<scratchpad>/ADDENDUM4.md   タグの色・パステル化
<scratchpad>/ADDENDUM5.md   引力のあるセル・並べ替え・ランダムの手ごたえ
```

色の計算に使った道具:

```
<scratchpad>/pal3/tint.mjs    染めたバブルの文字コントラスト（包絡線）
<scratchpad>/pal3/pairs.mjs   パレット45組を近い順に並べる
<scratchpad>/pal3/pick.mjs    1色を選び直す（帯ごとの最良を出す）
<scratchpad>/pal3/pick2.mjs   2色を同時に選び直す
```
